/** App info shape. Kept in entityTypes so renderer can import without pulling Node modules. */
export interface AppInfo {
  name: string;
  version: string;
  description: string;
  author: string;
}
