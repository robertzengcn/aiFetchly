---
phase: 17-hooks
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - src/service/hooks/hookFileFrontmatter.ts
  - src/service/aifetchlyConfig/AIFetchlyConfigLoader.ts
  - src/service/workspaceWatch/WorkspaceConfigScanner.ts
  - src/service/workspaceWatch/buildWorkspaceHookDefinitions.ts
  - src/service/aifetchlyConfig/AIFetchlyRuntimeRegistrySync.ts
  - src/service/workspaceWatch/WorkspaceTrustFilter.ts
  - src/service/workspaceWatch/WorkspaceWatchManagerSingleton.ts
  - test/vitest/main/service/AIFetchlyConfigLoader.hooks.test.ts
  - test/vitest/main/service/AIFetchlyRuntimeRegistrySync.hooks.test.ts
  - test/vitest/main/service/WorkspaceWatchManagerSingleton.trust.test.ts
autonomous: true
requirements: [HOK-01, TRS-02]
user_setup: []

must_haves:
  truths:
    - "A valid ~/.aifetchly/hooks/hooks.json produces CommandHookDefinition[] with scoped ids (user:hook:<index>) via the global loader; an invalid JSON or zod-failing entry yields a hooks-json-invalid diagnostic and the entry is skipped (HOK-01 parse)"
    - "A hooks.json entry whose event is outside the supported set yields an unsupported-event diagnostic and is skipped (HOK-02 SC4)"
    - "The workspace worker scans <workspace>/.aifetchly/hooks/hooks.json into raw drafts ONLY (no zod validation, no registry mutation, no DB) and the main-side buildWorkspaceHookDefinitions converter validates them (WAT-02 preserved)"
    - "applyWorkspaceSnapshot with trust.hooks=false drops every workspace hook BEFORE the hook registry is mutated; with trust.hooks=true the hooks pass through to HookRegistry.replaceSource (HOK-01 / SC1)"
    - "The manager's sync trustResolver reads an entity-backed cache hydrated from AIFetchlyWorkspaceTrustModule, NOT the Phase 14 approvalCache; revoking a workspace reflects immediately on the next worker event (Pitfall 2)"
    - "A single source exceeding maxHooksPerSource yields a count-cap diagnostic and surplus hooks are dropped (CFG-06)"
    - "An oversized hooks.json (> hooksJsonBytes) yields a file-too-large diagnostic and is not read (CFG-04)"
  artifacts:
    - "src/service/hooks/hookFileFrontmatter.ts (buildHookDefinition pure zod validator)"
    - "AIFetchlyConfigLoader.tryReadHookFiles (global single-JSON-file reader)"
    - "WorkspaceConfigScanner.tryReadHookFiles (worker raw-draft scanner, scan-only)"
    - "src/service/workspaceWatch/buildWorkspaceHookDefinitions.ts (main-side raw->definition converter)"
    - "AIFetchlyRuntimeRegistrySync hooks: trust-filter line + applySnapshot/removeSource hook wiring + hookRegistry field"
    - "WorkspaceTrustFilter entity-backed body"
    - "WorkspaceWatchManagerSingleton entity-backed sync trust cache (replaces approvalCache)"
  key_links:
    - "global loader -> snapshot.hooks[] (CommandHookDefinition[]) -> applySnapshot -> hookRegistry.replaceSource('user', ...)"
    - "worker scanner -> snapshot.hooks[] (raw drafts) -> applyWorkspaceSnapshot(trust) -> [trust.hooks? buildWorkspaceHookDefinitions : []] -> hookRegistry.replaceSource('workspace:<id>', ...)"
    - "AIFetchlyWorkspaceTrustModule.getTrust -> sync cache hydrate -> WorkspaceWatchManagerSingleton.trustResolver"
---

<objective>
Wire config-sourced hooks into the snapshot -> trust -> registry pipeline that Phases 13-16 established for commands/agents: parse `hooks/hooks.json` from global and trusted-workspace sources, validate it main-side, add the `hooks:` trust-filter line, and replace Phase 14's in-memory binary approvalCache with reads from the persisted AIFetchlyWorkspaceTrust entity (Plan 01).

Purpose: Make editing a trusted `<workspace>/.aifetchly/hooks/hooks.json` update dispatch behavior through HookRegistry.replaceSource (SC1) and persist workspace trust per-capability so revoke reflects immediately (Pitfall 2) — both via the established source-replacement + trust-filter patterns.
Output: Pure buildHookDefinition validator, global + worker hook scanners, main-side converter, the one-line trust filter + registry wiring, and the entity-backed sync trust cache.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-hooks/17-CONTEXT.md
@.planning/phases/17-hooks/17-RESEARCH.md
@.planning/phases/17-hooks/17-PATTERNS.md
@.planning/phases/17-hooks/17-VALIDATION.md
@CLAUDE.md

# Plan 01 outputs consumed here
@.planning/phases/17-hooks/17-01-PLAN.md

# Analog sources of truth (read before editing)
@src/service/aifetchlyConfig/AIFetchlyConfigLoader.ts
@src/service/workspaceWatch/WorkspaceConfigScanner.ts
@src/service/workspaceWatch/buildWorkspaceAgentDefinitions.ts
@src/service/workspaceWatch/WorkspaceWatchManagerSingleton.ts
@src/service/workspaceWatch/WorkspaceTrustFilter.ts
@src/service/aifetchlyConfig/AIFetchlyRuntimeRegistrySync.ts
@src/service/slashCommands/agentFrontmatter.ts
@src/entityTypes/hookTypes.ts
@src/entityTypes/aifetchlyConfigTypes.ts
@src/modules/AIFetchlyWorkspaceTrustModule.ts
@src/service/hooks/HookRegistry.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hooks.json parse layer — buildHookDefinition (pure zod) + global tryReadHookFiles + worker raw-draft scan + main-side converter + caps/diagnostics</name>
  <files>
    src/service/hooks/hookFileFrontmatter.ts,
    src/service/aifetchlyConfig/AIFetchlyConfigLoader.ts,
    src/service/workspaceWatch/WorkspaceConfigScanner.ts,
    src/service/workspaceWatch/buildWorkspaceHookDefinitions.ts,
    test/vitest/main/service/AIFetchlyConfigLoader.hooks.test.ts
  </files>
  <read_first>
    - src/service/slashCommands/agentFrontmatter.ts (buildAgentDefinition — the pure-validator analog: returns {ok:true,definition} | {ok:false,diagnostic}, never throws, no fs/DB/Electron)
    - src/service/aifetchlyConfig/AIFetchlyConfigLoader.ts lines 466-612 (tryReadAgentFiles — the template: stat -> size cap -> readFile -> file snapshot push -> parse -> validate per entry -> count cap -> diagnostic shape. Mirror this flow for a SINGLE hooks/hooks.json JSON file instead of a directory of .md)
    - src/service/workspaceWatch/WorkspaceConfigScanner.ts lines 596-727 (tryReadAgentFiles worker-side — the raw-draft template: scan-only, pushes bytes+hash+parsed-as-unknown, NO zod validation. Mirror for hooks.json)
    - src/service/workspaceWatch/buildWorkspaceAgentDefinitions.ts (the main-side raw-draft->definition converter template: loops drafts, calls buildHookDefinition, collects diagnostics, defense-in-depth try/catch never aborts the batch)
    - src/service/aifetchlyConfig/AIFetchlyConfigConstants.ts (Plan 01 added maxHooksPerSource:100 + hooksJsonBytes:128*1024 + codes hooks-json-invalid/unsupported-event/count-cap — import these, do NOT edit this file)
    - src/entityTypes/hookTypes.ts (CommandHookDefinition, HookEventName, HookSource enum — A3: workspace maps to "project"; the supported event set for zod is PreToolUse/PostToolUse/SessionStart/Stop)
    - src/entityTypes/aifetchlyConfigTypes.ts (AIFetchlyConfigDiagnostic shape: {severity, source, sourceId, filePath, code, message, recoverable}; WorkspaceHookDraft shape if present, else define it; AIFetchlyConfigFileKind includes "hook" at line ~35)
    - src/service/aifetchlyConfig/resolveConfigRelativePath.ts (CFG-05 path-safety helper — reuse for the hooks file path)
    - .planning/phases/17-hooks/17-RESEARCH.md § Code Examples (buildHookDefinition full zod schema + the unsupported-event handling; hookEntrySchema with the command-or-skill refine)
    - .planning/phases/17-hooks/17-PATTERNS.md (hookFileFrontmatter.ts, AIFetchlyConfigLoader, WorkspaceConfigScanner, buildWorkspaceHookDefinitions analog rows — all exact)
  </read_first>
  <behavior>
    - Valid hooks.json: an array of entries each with event PreToolUse|PostToolUse|SessionStart|Stop + (command XOR skill) -> buildHookDefinition returns {ok:true, definition} with id `${sourceId}:hook:${index}`.
    - Command entry: definition.type === "command", definition.command set, trusted === (source==='user'); workspace-sourced definition carries source:"project" enum (A3).
    - Skill-ref entry: definition registered with command placeholder "true"; it parses and validates but resolves to a skill-registry-not-available diagnostic only at FIRE time (Plan 03), not at parse time (this task registers it).
    - Invalid JSON: a hooks.json that fails JSON.parse -> hooks-json-invalid diagnostic, no definitions, no throw.
    - Zod failure: an entry missing both command and skill, or with an out-of-range event (e.g. "PermissionRequest") -> per-entry diagnostic; for out-of-range event the code is unsupported-event; for shape failure the code is hooks-json-invalid; the batch continues with the remaining entries.
    - Size cap (CFG-04): a hooks.json whose stat size > hooksJsonBytes -> file-too-large diagnostic, file is NOT read.
    - Count cap (CFG-06): more than maxHooksPerSource valid entries from one source -> count-cap diagnostic, surplus entries dropped.
    - Worker scan-only (WAT-02): WorkspaceConfigScanner.tryReadHookFiles pushes raw drafts (bytes + hash + parsed-JSON-as-unknown) and performs NO zod validation; buildWorkspaceHookDefinitions validates main-side.
    - Pure validator: buildHookDefinition has zero fs/DB/Electron imports (grep-clean).
  </behavior>
  <action>
    Create `src/service/hooks/hookFileFrontmatter.ts` — a PURE module (no fs/DB/Electron; mirror agentFrontmatter's contract). Export `buildHookDefinition(raw, sourceMeta: {source:'user'|'workspace', sourceId, relativePath}, index)` returning `{ok:true; definition: CommandHookDefinition} | {ok:false; diagnostic: AIFetchlyConfigDiagnostic}`. Define a `hookEntrySchema` zod object (strict): event enum [PreToolUse, PostToolUse, SessionStart, Stop], matcher optional string max 128, command optional non-empty string, skill optional non-empty string (D-Vocabulary), timeoutMs optional int positive max 60000, cwd optional string, envAllowlist optional string array, enabled optional boolean, failureMode optional enum [warn, block]. Add a `.refine` requiring command OR skill (reject both-empty with a hooks-json-invalid diagnostic). NEVER throw — on safeParse failure, map to a diagnostic with code hooks-json-invalid (shape) or unsupported-event (event outside enum — though the zod enum already rejects unknown events as unsupported-event via a custom error map; ensure the diagnostic code is unsupported-event specifically for the event field, hooks-json-invalid for other fields). Build the CommandHookDefinition: id `${sourceMeta.sourceId}:hook:${index}`; eventName from parsed.event; source mapped to "project" when sourceMeta.source==='workspace' else "user" (A3 resolved); trusted true for user, false for workspace (trust is enforced separately by the trust filter — the registry's defense-in-depth getMatchingHooks skip of untrusted command hooks stays); failureMode default "warn"; for skill-ref entries set type "command" with a placeholder command so the registry stores it, and the Plan 03 dispatcher emits the skill-registry-not-available diagnostic at fire time (do NOT emit that diagnostic at parse time — the hook is intentionally registered as a documented no-op per D-Vocabulary).

    Add `tryReadHookFiles` to `AIFetchlyConfigLoader` (global, `~/.aifetchly/hooks/hooks.json` — a SINGLE JSON file, NOT a directory). Mirror tryReadAgentFiles L466-612: stat the file -> CFG-04 size check against hooksJsonBytes (file-too-large diagnostic + skip read) -> readFile -> push an AIFetchlyConfigFileSnapshot with kind:"hook" -> JSON.parse (hooks-json-invalid diagnostic on failure) -> validate the top-level is an array -> iterate entries calling buildHookDefinition with index -> enforce maxHooksPerSource count cap (count-cap diagnostic + break) -> push definitions into the hooks[] array and diagnostics into diagnostics[]. Reuse resolveConfigRelativePath for CFG-05 path safety. Call tryReadHookFiles from the loader's main scan alongside tryReadAgentFiles so global hooks populate snapshot.hooks[] with source:"user".

    Add `tryReadHookFiles` to `WorkspaceConfigScanner` (worker-side, `<workspace>/.aifetchly/hooks/hooks.json`). SCAN-ONLY: mirror WorkspaceConfigScanner.tryReadAgentFiles L596-727 — stat + size cap + readFile + push a RAW draft (bytes + content hash + parsed-JSON-as-unknown) onto snapshot.hooks[] and a kind:"hook" file entry. Do NOT call buildHookDefinition or any zod schema in the worker (WAT-02 — validation is main-side). Push ioDiagnostic on stat/read failure.

    Create `src/service/workspaceWatch/buildWorkspaceHookDefinitions.ts` — the main-side converter (mirror buildWorkspaceAgentDefinitions.ts). `buildWorkspaceHookDefinitions(drafts: readonly WorkspaceHookDraft[], workspaceId)` -> `{definitions: CommandHookDefinition[]; diagnostics: AIFetchlyConfigDiagnostic[]}`. Loop drafts, call buildHookDefinition with sourceMeta {source:'workspace', sourceId:`workspace:${workspaceId}`}, collect results, defense-in-depth try/catch (scanner-io-error diagnostic on throw, never abort the batch). Define the WorkspaceHookDraft type in aifetchlyConfigTypes if not already present (mirror WorkspaceAgentDraft: {raw: unknown; hash: string; path: string; index: number}).

    Auto-commit per file (validator, global loader, worker scanner, converter).
  </action>
  <verify>
    <automated>AIFETCHLY_SKIP_TSC=1 npx vitest run --config vite.main.config.mjs test/vitest/main/service/AIFetchlyConfigLoader.hooks.test.ts -x && AIFETCHLY_SKIP_TSC=1 npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - AIFetchlyConfigLoader.hooks.test.ts green for: valid-array parse, command entry shape, skill-ref entry registered (no parse-time diagnostic), invalid-JSON -> hooks-json-invalid, zod-shape-failure -> hooks-json-invalid, out-of-range event -> unsupported-event, oversized file -> file-too-large (not read), count > maxHooksPerSource -> count-cap + surplus dropped.
    - buildHookDefinition purity: `grep -rn "require('fs')\|from 'fs'\|from 'typeorm'\|from 'electron'\|SqliteDb\|BaseDb" src/service/hooks/hookFileFrontmatter.ts` returns nothing (zero fs/DB/Electron imports).
    - Worker scan-only: `grep -c "buildHookDefinition\|hookEntrySchema" src/service/workspaceWatch/WorkspaceConfigScanner.ts` returns 0 (the worker does NOT validate — WAT-02).
    - Global loader reads a SINGLE file: the implementation joins rootPath + hooks dir + "hooks.json" (one file), not a directory glob.
    - `grep -c "maxHooksPerSource\|hooksJsonBytes" src/service/aifetchlyConfig/AIFetchlyConfigLoader.ts` >= 2 (both caps enforced).
    - A3 mapping: a workspace-sourced valid entry yields a definition with `source === "project"` (HookSource enum), verified by a unit assertion.
    - `npx tsc --noEmit` 0 errors.
  </acceptance_criteria>
  <done>
    hooks.json is parsed from global config into validated CommandHookDefinition[] (source "user"); the worker snapshots raw workspace drafts (scan-only, WAT-02) which buildWorkspaceHookDefinitions validates main-side; CFG-04 size + CFG-06 count caps enforced; invalid/unsupported/oversized/too-many all produce the correct closed-set diagnostic codes; skill-ref entries register as documented no-ops.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: hooks: trust-filter line + applySnapshot/removeSource registry wiring + entity-backed sync trust cache replacing approvalCache (HOK-01 filter + TRS-02 cache; Pitfall 2 + Pitfall 5)</name>
  <files>
    src/service/aifetchlyConfig/AIFetchlyRuntimeRegistrySync.ts,
    src/service/workspaceWatch/WorkspaceTrustFilter.ts,
    src/service/workspaceWatch/WorkspaceWatchManagerSingleton.ts,
    test/vitest/main/service/AIFetchlyRuntimeRegistrySync.hooks.test.ts,
    test/vitest/main/service/WorkspaceWatchManagerSingleton.trust.test.ts
  </files>
  <read_first>
    - src/service/aifetchlyConfig/AIFetchlyRuntimeRegistrySync.ts (FULL file: the constructor + fields — commandRegistry, agentRegistry, contextStore; applyWorkspaceSnapshot at ~L152-168 with instructions/commands/agents trust lines; applySnapshot body — find where agents are converted + agentRegistry.replaceSource is called and mirror it for hooks; removeSource at ~L175-179 calling commandRegistry.replaceSource([]) + agentRegistry.replaceSource([]) + contextStore.removeSource)
    - src/service/workspaceWatch/WorkspaceWatchManagerSingleton.ts (FULL file: approvalCache Map<string,boolean> ~L63; trustResolver closure ~L89 reading approvalCache.get(workspaceId) ?? false; markWorkspaceApproved ~L106; initWorkspaceWatchManager constructor — this is where you hydrate the entity-backed cache and replace the trustResolver body)
    - src/service/workspaceWatch/WorkspaceTrustFilter.ts (derivePhase14Trust body ~L35-45 — replace its BODY, keep the export signature so callers don't change; the file's own comment foreshadows this replacement)
    - src/service/AgentDefinitionRegistry.ts + src/service/hooks/HookRegistry.ts (Plan 01 added replaceSource/unregisterSource — the methods you call here)
    - src/modules/AIFetchlyWorkspaceTrustModule.ts (Plan 01 — getTrust/setTrust/ensureMigrationSeed; you hydrate the sync cache from getTrust)
    - src/service/workspaceWatch/WorkspaceResolver.ts (the async resolver whose normalization the entity hashes must match — Pitfall 2/5 correctness depends on workspaceId -> rootPath -> rootHash alignment)
    - src/entityTypes/aifetchlyConfigTypes.ts (AIFetchlySourceTrust — the 5-field shape the cache holds; the hooks+skills fields already exist)
    - .planning/phases/17-hooks/17-RESEARCH.md (Pitfall 2 revoke-reflects; Pitfall 5 sync/async bridge — hydrate sync map at startup after seed + on every write; Open Q2 resolved: hydrate at initWorkspaceWatchManager + on markWorkspaceApproved/revoke)
    - .planning/phases/17-hooks/17-PATTERNS.md (Pattern 2 trust-filter line; WorkspaceWatchManagerSingleton + WorkspaceTrustFilter analog rows)
  </read_first>
  <behavior>
    - Trust filter (HOK-01): applyWorkspaceSnapshot(snapshot, {hooks:false,...}) produces a filtered snapshot whose hooks[] is empty -> applySnapshot does NOT call hookRegistry.replaceSource with workspace hooks (they are dropped BEFORE mutation). With {hooks:true,...} the workspace hooks pass through to replaceSource.
    - Registry wiring (SC1): applySnapshot, given a snapshot with hooks[] definitions + a trusted workspace source, calls hookRegistry.replaceSource(`workspace:<id>`, definitions); on an empty hooks[] it calls replaceSource(sourceId, []) (reconciliation removes stale hooks).
    - removeSource: calling removeSource(`workspace:<id>`) clears that source's hooks from the registry (hookRegistry.replaceSource(sourceId, []) or unregisterSource).
    - Entity-backed cache (TRS-02): the manager's trustResolver reads a sync Map populated from AIFetchlyWorkspaceTrustModule.getTrust, NOT the old approvalCache. A workspace with trustHooks=true in the entity resolves to a trust object with hooks=true; an absent/false row resolves to hooks=false (fail-closed).
    - Revoke reflects immediately (Pitfall 2): after the Module sets all flags false (revoke), the next worker event's trustResolver returns hooks=false WITHOUT an app restart (Phase 14's documented limitation is removed). The revoke path triggers a rescan so applyWorkspaceSnapshot drops the hooks via replaceSource(sourceId, []).
    - Sync/async bridge (Pitfall 5): trustResolver is synchronous; it reads the in-memory map. The map is hydrated async at initWorkspaceWatchManager (after ensureMigrationSeed) and re-hydrated on every markWorkspaceApproved/revoke write. No Promise is returned from trustResolver.
    - markWorkspaceApproved preserves its call signature; internally it writes all-5-flags-true via the Module (D-TrustUX block-write) and re-hydrates the cache.
  </behavior>
  <action>
    AIFetchlyRuntimeRegistrySync: add a `hookRegistry: HookRegistryApi` dependency (constructor param + field) — mirror how commandRegistry/agentRegistry are injected; update the constructor's caller (the AIFetchlyConfigManager / wherever the sync is constructed — read it to find the construction site and pass the existing HookRegistry singleton). In `applyWorkspaceSnapshot`, add the one-line trust filter `hooks: trust.hooks ? snapshot.hooks : []` mirroring the agents line at ~L165 (Pattern 2). In `applySnapshot`, mirror the agents apply path: for trusted-workspace raw drafts call buildWorkspaceHookDefinitions then `this.hookRegistry.replaceSource(snapshot.sourceId, definitions)`; for the global source the snapshot.hooks[] already holds CommandHookDefinition[] so call `this.hookRegistry.replaceSource('user', snapshot.hooks)` directly (match exactly how agents distinguish global definitions vs workspace drafts in the existing applySnapshot code — read that branch and replicate the shape). In `removeSource`, add `this.hookRegistry.replaceSource(sourceId, [])` (or unregisterSource) alongside the existing command/agent calls.

    WorkspaceTrustFilter: replace the BODY of derivePhase14Trust (keep the export signature so callers don't change). Under D-TrustUX all 5 flags track the same binary approval, but the read path must now consult an entity-backed source. Accept the already-resolved AIFetchlySourceTrust (the caller — the manager — resolves it from the sync cache before calling) OR become a thin pass-through that reads the sync cache. Do NOT hardcode hooks/skills to false anymore — flow whatever the entity says. (Confirm which shape the caller passes by reading WorkspaceWatchManager's applySnapshotCallback invocation.)

    WorkspaceWatchManagerSingleton: replace the write-only `approvalCache: Map<string, boolean>` with an entity-backed `Map<workspaceId, AIFetchlySourceTrust>` sync-read cache. Resolution of Open Q2 — hydrate at TWO points: (1) inside `initWorkspaceWatchManager` after constructing the Module, call ensureMigrationSeed() then load all trust rows + the WorkspaceEntity id->rootPath map to populate the cache keyed by workspaceId (the entity is keyed by workspaceRootHash, so join workspaceId -> rootPath -> normalized hash -> entity row; mirror WorkspaceResolver's normalization exactly so hashes match); (2) re-hydrate the affected entry on every markWorkspaceApproved / revoke write. The `trustResolver` closure becomes `(workspaceId) => trustCache.get(workspaceId) ?? ALL_FALSE` (sync — Pitfall 5 bridge; the map is the sync-read cache, the entity is the durable source). `markWorkspaceApproved(workspaceId)` becomes a thin wrapper: look up the rootPath, call `AIFetchlyWorkspaceTrustModule.setTrust(rootHash, rootPath, ALL_TRUE)` (D-TrustUX block write), then re-hydrate that cache entry. Add (or wire to the existing) revoke path: set all flags false via the Module + re-hydrate + trigger a rescan so applyWorkspaceSnapshot drops the hooks (Pitfall 2). Keep the manager's sync contract — trustResolver returns AIFetchlySourceTrust (or boolean if the manager signature truly requires boolean; if so, derive boolean = trust.hooks from the entity row so the per-capability read path still flows — read the manager's trustResolver type signature and preserve it, returning the hooks-flag-derived value while the sync cache holds the full 5-field object for future per-capability use).

    Auto-commit per concern (sync wiring, trust filter body, singleton cache).
  </action>
  <verify>
    <automated>AIFETCHLY_SKIP_TSC=1 npx vitest run --config vite.main.config.mjs test/vitest/main/service/AIFetchlyRuntimeRegistrySync.hooks.test.ts test/vitest/main/service/WorkspaceWatchManagerSingleton.trust.test.ts -x && AIFETCHLY_SKIP_TSC=1 npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - AIFetchlyRuntimeRegistrySync.hooks.test.ts green for: trust.hooks=false drops hooks before mutation (replaceSource NOT called with workspace hooks); trust.hooks=true passes through (replaceSource called); applySnapshot reconciles via hookRegistry.replaceSource(sourceId, defs); removeSource clears hooks; a rescan with changed hooks replaces atomically (SC1).
    - WorkspaceWatchManagerSingleton.trust.test.ts green for: trustResolver reads the entity-backed cache (NOT approvalCache); an approved workspace resolves trust.hooks=true; an absent/false row resolves false (fail-closed); revoke sets flags false + the NEXT worker event sees hooks=false without restart (Pitfall 2); markWorkspaceApproved writes all-5-true via the Module + re-hydrates.
    - `grep -c "approvalCache" src/service/workspaceWatch/WorkspaceWatchManagerSingleton.ts` returns 0 (the old cache is gone — D-Migration / Pitfall 2).
    - `grep -c "AIFetchlyWorkspaceTrustModule\|trustCache" src/service/workspaceWatch/WorkspaceWatchManagerSingleton.ts` >= 1 (entity-backed cache present).
    - `grep -c "trust.hooks ? snapshot.hooks" src/service/aifetchlyConfig/AIFetchlyRuntimeRegistrySync.ts` returns 1 (the hooks trust-filter line exists exactly once).
    - `grep -c "hookRegistry" src/service/aifetchlyConfig/AIFetchlyRuntimeRegistrySync.ts` >= 2 (field + usage in applySnapshot/removeSource).
    - trustResolver is sync: `grep -n "trustResolver" src/service/workspaceWatch/WorkspaceWatchManagerSingleton.ts` shows no `async`/`await` inside the resolver closure (Pitfall 5).
    - `npx tsc --noEmit` 0 errors.
  </acceptance_criteria>
  <done>
    The hooks trust-filter line drops untrusted workspace hooks before registry mutation; applySnapshot/removeSource wire HookRegistry.replaceSource for the hooks source; the Phase 14 approvalCache is fully replaced by an entity-backed sync cache hydrated from AIFetchlyWorkspaceTrustModule; revoke reflects on the next worker event without restart; trustResolver stays synchronous.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker (raw drafts) -> main (validation + registry mutation) | raw workspace hook JSON crosses from the scan-only worker into main-side validation + trust gating |
| DB (entity) -> sync cache -> trustResolver | persisted trust crosses from SQLite into the sync read path (Pitfall 5 bridge) |
| hooks.json author input -> HookRegistry | untrusted author-declared hooks/matchers/commands cross into the registry |

## STRIDE Threat Register (ASVS L1; block-on-high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-05 | Tampering | hooks.json parsing (buildHookDefinition) | high | mitigate | zod strict schema validates every field; command-or-skill refine rejects both-empty; out-of-range event -> unsupported-event diagnostic + skip; CFG-04 size cap via stat before read; CFG-06 count cap drops surplus; CFG-05 resolveConfigRelativePath on the file path. Worker is scan-only — validation is main-side (WAT-02). (V5 Input Validation, V12 Files & Resources.) |
| T-17-06 | Elevation | applyWorkspaceSnapshot trust filter | high | mitigate | `hooks: trust.hooks ? snapshot.hooks : []` drops untrusted workspace hooks BEFORE applySnapshot mutates the registry (TRS-01/TRS-02). Trust is read from the persisted entity, not a UI-only flag. Defense-in-depth: HookRegistry.getMatchingHooks already skips untrusted command hooks (V4 Access Control). |
| T-17-07 | Tampering | trustResolver (Pitfall 2 stale trust) | high | mitigate | Entity-backed sync cache hydrated at startup (after ensureMigrationSeed) + re-hydrated on every markWorkspaceApproved/revoke; revoke sets all flags false + triggers rescan -> applyWorkspaceSnapshot -> replaceSource(sourceId, []) drops the hooks. Phase 14's "stale until restart" limitation is removed. |
| T-17-08 | Tampering | sync/async trust bridge (Pitfall 5) | medium | mitigate | trustResolver stays synchronous; reads an in-memory Map hydrated from the async entity. The entity is the durable source; the map is a sync-read cache. No Promise returned from the resolver — TypeScript would reject it. |
| T-17-09 | Tampering | WorkspaceTrustFilter.derivePhase14Trust | medium | mitigate | Body replaced to consult the entity (no longer hardcoded false for hooks/skills); export signature preserved so no caller bypass. All-flags-false is the fail-closed default for absent rows. |
| T-17-SC | Tampering | package installs | n/a | accept | Zero new packages this plan (RESEARCH § Package Legitimacy Audit). |

</threat_model>

<verification>
- `AIFETCHLY_SKIP_TSC=1 npx vitest run --config vite.main.config.mjs test/vitest/main/service/AIFetchlyConfigLoader.hooks.test.ts test/vitest/main/service/AIFetchlyRuntimeRegistrySync.hooks.test.ts test/vitest/main/service/WorkspaceWatchManagerSingleton.trust.test.ts -x` green.
- `npx tsc --noEmit` 0 errors.
- Grep gates: approvalCache gone from singleton; hookRegistry wired in sync; trust filter line present exactly once; worker scan-only (no buildHookDefinition in WorkspaceConfigScanner); validator pure (no fs/DB/Electron).
- Do NOT run bare `yarn testmain` (RESEARCH Pitfall 6).
</verification>

<success_criteria>
- HOK-01 (parse + filter + registry wiring): hooks.json parsed from global + workspace; untrusted workspace hooks dropped before mutation; HookRegistry.replaceSource reconciles on rescan (SC1 live-update path is wired end-to-end at the registry level; full live dispatch UAT is manual per VALIDATION Manual-Only).
- TRS-02 (cache replacement): the Phase 14 approvalCache is replaced by the persisted AIFetchlyWorkspaceTrust-backed sync cache; revoke reflects immediately (Pitfall 2); the sync contract is preserved (Pitfall 5).
</success_criteria>

<output>
Create `.planning/phases/17-hooks/17-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 02 contribution)

| Artifact | Kind | File | Purpose |
|----------|------|------|---------|
| `buildHookDefinition` | pure zod validator | src/service/hooks/hookFileFrontmatter.ts | hooks.json entry -> CommandHookDefinition or diagnostic |
| `hookEntrySchema` | zod schema | src/service/hooks/hookFileFrontmatter.ts | strict shape + command-or-skill refine |
| `AIFetchlyConfigLoader.tryReadHookFiles` | loader method | src/service/aifetchlyConfig/AIFetchlyConfigLoader.ts | global ~/.aifetchly/hooks/hooks.json -> snapshot.hooks[] |
| `WorkspaceConfigScanner.tryReadHookFiles` | scanner method | src/service/workspaceWatch/WorkspaceConfigScanner.ts | worker raw-draft scan (scan-only, WAT-02) |
| `buildWorkspaceHookDefinitions` | converter | src/service/workspaceWatch/buildWorkspaceHookDefinitions.ts | main-side raw -> CommandHookDefinition |
| `hooks: trust.hooks ? snapshot.hooks : []` | trust-filter line | src/service/aifetchlyConfig/AIFetchlyRuntimeRegistrySync.ts | drop untrusted hooks before mutation |
| `hookRegistry` field + applySnapshot/removeSource wiring | sync integration | src/service/aifetchlyConfig/AIFetchlyRuntimeRegistrySync.ts | route trusted hooks into HookRegistry.replaceSource |
| entity-backed sync trust cache | cache | src/service/workspaceWatch/WorkspaceWatchManagerSingleton.ts | replaces approvalCache (TRS-02 / D-Migration) |
| `derivePhase14Trust` entity-backed body | filter | src/service/workspaceWatch/WorkspaceTrustFilter.ts | per-capability read from the entity |

## Assumptions / open items resolved this plan
- A3 (HookSource enum): workspace -> "project" enum value, applied in buildHookDefinition (SOURCE_PRIORITY project:3 < user:5).
- Open Q2 (sync trustResolver hydration timing): hydrate at initWorkspaceWatchManager after ensureMigrationSeed + on every markWorkspaceApproved/revoke write.
- Pitfall 2 (stale trust after revoke): entity + immediate re-hydrate + rescan removes Phase 14's restart limitation.
- Pitfall 5 (sync/async bridge): sync Map hydrated from the async entity; trustResolver stays sync.
- Open Q3 (/hooks command): DEFERRED — out of HOK-01/02 success criteria; /status already shows hook count.
