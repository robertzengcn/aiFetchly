---
phase: 17-hooks
plan: 03
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - src/childprocess/hook-execution/workerProtocol.ts
  - src/childprocess/hook-execution/HookExecutionWorker.ts
  - src/service/hooks/hookExecutionClient.ts
  - forge.config.js
  - vite.hookExecutionWorker.config.mjs
  - src/service/hooks/HookDispatcher.ts
  - src/service/StreamEventProcessor.ts
  - test/vitest/utilitycode/hooks/HookExecutionWorker.test.ts
  - test/vitest/utilitycode/hooks/HookDispatcher.test.ts
autonomous: true
requirements: [HOK-02]
user_setup: []

must_haves:
  truths:
    - "Config-sourced command hooks NEVER execute in the Electron main process: the HookDispatcher command-hook branch routes through a worker round-trip instead of performing in-process child execution (HOK-02 SC2)"
    - "The hook-execution worker receives an execute-hook command, runs a worker-local spawn-core helper (shell-disabled, env allowlist, timeout, stdout/stderr caps, JSON output validation) adapted from CommandHookExecutor, and returns a hook-result event (HOK-02)"
    - "The worker is a long-lived lazy singleton (forked once on first command-hook dispatch, not per firing) so the PreToolUse hot path stays sub-millisecond when no hooks match and cheap when one does (Pitfall 1)"
    - "The worker imports NO database/ORM/Electron/registry code AND does NOT import HookCommandTrustService or call setTrusted — trust is decided in main before dispatch (WAT-02 for the new worker dir)"
    - "A skill-referenced hook (D-Vocabulary) registers but at fire time emits a non-fatal skill-registry-not-available diagnostic and performs NO execution (HOK-02 SC4)"
    - "Hook failures, timeouts, and malformed worker messages are non-fatal: they surface as diagnostics / aggregator hookErrors and NEVER crash the AI stream (HOK-02 SC4)"
    - "SessionStart and Stop hook events are live (emitted at stream start and completion), completing HOK-01 event coverage (A5 resolved)"
  artifacts:
    - "src/childprocess/hook-execution/workerProtocol.ts (zod execute-hook command + hook-result event + shutdown)"
    - "src/childprocess/hook-execution/HookExecutionWorker.ts (fork entry with worker-local spawn-core helper)"
    - "src/service/hooks/hookExecutionClient.ts (main-side lazy-singleton IPC client, hookRunId correlation)"
    - "forge.config.js hook-execution worker entry + vite.hookExecutionWorker.config.mjs"
    - "HookDispatcher command-hook -> worker round-trip branch + skill-ref no-op branch"
    - "StreamEventProcessor SessionStart/Stop emit sites"
  key_links:
    - "HookDispatcher (main) -> hookExecutionClient.execute -> IPC execute-hook -> HookExecutionWorker spawn-core -> IPC hook-result -> HookDispatcher -> HookResultAggregator"
    - "StreamEventProcessor stream-start -> HookDispatcher.executeHooks(SessionStart) ; stream-completion -> executeHooks(Stop)"
    - "malformed hook-result / timeout -> synthesized non-fatal failure result (never throws into the stream)"
---

<objective>
Route config-sourced command-hook execution through a NEW dedicated worker so the Electron main process never performs child execution for hooks (HOK-02 SC2), keep the worker DB/Electron/trust-service-free (WAT-02), make hook failures non-fatal, wire the skill-reference no-op diagnostic (D-Vocabulary), and light up the SessionStart/Stop emitters so all four HOK-01 events are live (A5).

Purpose: Satisfy the hard constraint that no config-sourced hook command runs in the main process, while reusing the existing finished executor/validator/aggregator instead of rewriting them. The worker is the only HOK-02-compliant mechanism (RESEARCH settled Option (b); reusing the scan worker was rejected for lifecycle + invariant reasons, an in-process allowlist was eliminated for directly violating SC2).
Output: Worker protocol + entry with worker-local spawn-core (build config registered), dispatcher command-hook branch routed through the worker, skill-ref no-op, SessionStart/Stop emitters, WAT-02 + TRS-05 + SC2 grep gates green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-hooks/17-CONTEXT.md
@.planning/phases/17-hooks/17-RESEARCH.md
@.planning/phases/17-hooks/17-PATTERNS.md
@.planning/phases/17-hooks/17-VALIDATION.md
@CLAUDE.md

# Plan 01 outputs consumed here
@.planning/phases/17-hooks/17-01-PLAN.md

# Analog sources of truth (read before editing)
@src/childprocess/aifetchly-config/WorkspaceConfigWatchWorker.ts
@src/service/workspaceWatch/WorkspaceWatchProtocol.ts
@src/service/workspaceWatch/WorkspaceWatchManager.ts
@src/service/hooks/HookDispatcher.ts
@src/service/hooks/executors/CommandHookExecutor.ts
@src/service/hooks/HookResultAggregator.ts
@src/service/hooks/HookOutputValidator.ts
@src/service/hooks/builtinHooks.ts
@src/service/StreamEventProcessor.ts
@src/entityTypes/hookTypes.ts
@src/service/aifetchlyConfig/AIFetchlyConfigConstants.ts
@forge.config.js
@vite.aifetchlyConfigWorker.config.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hook-execution worker — zod protocol + fork entry with worker-local spawn-core helper (adapted from CommandHookExecutor) + main client + forge/vite build registration</name>
  <files>
    src/childprocess/hook-execution/workerProtocol.ts,
    src/childprocess/hook-execution/HookExecutionWorker.ts,
    src/service/hooks/hookExecutionClient.ts,
    forge.config.js,
    vite.hookExecutionWorker.config.mjs,
    test/vitest/utilitycode/hooks/HookExecutionWorker.test.ts
  </files>
  <read_first>
    - src/service/workspaceWatch/WorkspaceWatchProtocol.ts (FULL — the zod discriminated-union pattern: strict() objects, workerCommandSchema/workerEventSchema exports, safeParse both directions. Clone this shape for execute-hook/hook-result)
    - src/childprocess/aifetchly-config/WorkspaceConfigWatchWorker.ts lines 196-243 (the fork entry pattern: initializeWorker with process.on('message') -> safeParse -> handleCommand -> catch->emit error; process.on('uncaughtException'/'unhandledRejection'); the WORKER_TYPE env bootstrap guard at the bottom)
    - src/service/workspaceWatch/WorkspaceWatchManager.ts (the fork + message round-trip lifecycle pattern the hookExecutionClient clones: how it forks the worker entry, correlates by an id, enforces timeout, kills the child on abort)
    - src/service/hooks/executors/CommandHookExecutor.ts lines 76-271 (THE TEMPLATE for the worker-local spawn-core helper — read this to ADAPT, not to import. It contains: parseCommand whitespace split L65-74; spawn child creation L118-135 with shell:false + cwd + env; timeout timer + SIGKILL on expiry L144-151; abortSignal -> SIGKILL L153-160; stdout byte cap + kill L162-174; stderr byte cap L176-181; finalize() with timedOut/aborted/stdout-cap/JSON.parse/validateHookOutput L183-236; child.on('error') ENOENT synthesis L238-254; child.on('close') -> finalize L256-262; stdin write L264-269. CRITICAL: do NOT import executeCommand whole — at L83 it gates on HookCommandTrustService.isTrusted(hook.id), a MAIN-PROCESS trust decision; the worker gets a separate instance where setTrusted is never called so the gate would ALWAYS fail. ALSO its signature executeCommand({hook, input, abortSignal}) does NOT match the IPC protocol's {command, cwd, envAllowlist, timeoutMs, stdinPayload} shape. The worker must implement its OWN spawn-core function consuming the IPC payload, reusing the spawn/timeout/caps/validateHookOutput LOGIC but NOT the trust gate.)
    - src/service/hooks/HookOutputValidator.ts (validateHookOutput — imported unchanged inside the worker's spawn-core helper)
    - src/entityTypes/hookTypes.ts (HOOK_LIMITS constants for maxCommandTimeoutMs/defaultCommandTimeoutMs/maxCommandStdoutBytes/maxCommandStderrBytes + DEFAULT_HOOK_ENV_KEYS — the spawn-core helper reads these; CommandHookExecutionResult shape the worker maps its result to)
    - forge.config.js lines 395-415 (the worker entry registration pattern — PythonRuntimeWorker / aifetchly-config / contact-extraction entries)
    - vite.aifetchlyConfigWorker.config.mjs (the vite worker config to clone verbatim, swapping entry + output names)
    - .planning/phases/17-hooks/17-RESEARCH.md (Execution-Boundary Mechanism Decision — Option (b); worker protocol recommended shape; Pitfall 1 lazy singleton; Pitfall 3 shell-disabled splitter limitation inherited unchanged; CommandHookExecutor spawn core reused via worker-local adaptation, NOT whole import)
    - .planning/phases/17-hooks/17-PATTERNS.md (HookExecutionWorker + workerProtocol + hookExecutionClient analog rows)
  </read_first>
  <behavior>
    - Protocol validity: a well-formed execute-hook command (type, hookRunId, command, stdinPayload) parse-checks green; a hook-result event with stdout/stderr/durationMs parse-checks green; a smuggled extra field is REJECTED (strict()).
    - Worker execute: given execute-hook, the worker runs its OWN worker-local spawn-core helper (adapted from CommandHookExecutor L76-271) and emits hook-result with the helper's stdout/stderr/durationMs; on helper timeout, hook-result.error.timedOut is true.
    - Worker spawn-core has NO trust gate: the helper does NOT import HookCommandTrustService and does NOT call isTrusted/setTrusted — trust is decided in MAIN (HookDispatcher / HookCommandTrustService main-side) BEFORE the execute-hook IPC is sent; the worker only executes what main already trusted.
    - Worker shell-disabled: the helper's child execution uses shell:false (inherited from CommandHookExecutor's spawn call at L121) — verified by reading the adapted code, NOT by reintroducing shell.
    - Worker isolation (WAT-02): the worker dir imports NO database / ORM / Electron / main-process registry / model / HookCommandTrustService code.
    - Malformed inbound: a message failing workerCommandSchema.safeParse is dropped with a console.warn (never crashes the worker); the dispatcher side synthesizes a non-fatal failure for any hookRunId that never gets a hook-result.
    - Main client lazy singleton (Pitfall 1): the first call to the client forks the worker ONCE; subsequent calls reuse it (one IPC round-trip per firing, not a fork). The client correlates request/response by hookRunId, enforces timeoutMs (kills the child side), honors abortSignal, and on malformed/missing hook-result synthesizes a non-fatal failure result (never throws into the stream).
    - Env allowlist: only DEFAULT_HOOK_ENV_KEYS flow to the child; process.env is never spread (information-disclosure mitigation).
  </behavior>
  <action>
    Create `src/childprocess/hook-execution/workerProtocol.ts` — clone the WorkspaceWatchProtocol.ts zod pattern. Define `executeHookCommandSchema` (strict): type literal "execute-hook", hookRunId non-empty string, command non-empty string, cwd optional string, envAllowlist optional string array, timeoutMs optional int positive max 60000, stdinPayload string (JSON.stringify of the hook input). Define `shutdownCommandSchema` (strict): type literal "shutdown". Export `workerCommandSchema = z.discriminatedUnion("type", [executeHookCommandSchema, shutdownCommandSchema])` and `HookExecutionCommand` type. Define `hookResultEventSchema` (strict): type literal "hook-result", hookRunId non-empty, stdout string, stderr string, durationMs int nonnegative, error optional {message string, timedOut optional boolean}. Export `workerEventSchema = z.discriminatedUnion("type", [hookResultEventSchema])` and `HookExecutionEvent` type. Both directions safeParse'd.

    Create `src/childprocess/hook-execution/HookExecutionWorker.ts` — clone the WorkspaceConfigWatchWorker.ts entry-lifecycle pattern (request/response variant). emit() via process.send. handleCommand switches on cmd.type: "execute-hook" -> run the worker-local spawn-core helper and emit hook-result; "shutdown" -> return. initializeWorker wires process.on('message', raw -> workerCommandSchema.safeParse -> handleCommand -> catch emits a hook-result error), process.on('uncaughtException'/'unhandledRejection') emitting an error + exiting (mirror lines 219-231). Bootstrap guard: `if (require.main === module || process.env.WORKER_TYPE === 'hook-execution') initializeWorker()` and `export { initializeWorker }`.

    MANDATORY worker-local spawn-core helper (function inside HookExecutionWorker.ts, NOT an import of executeCommand): adapt the spawn/timeout/caps/validate logic from CommandHookExecutor.ts L76-271 into a function whose INPUT is the execute-hook IPC payload `{command, cwd?, envAllowlist?, timeoutMs?, stdinPayload}` (NOT the {hook, input, abortSignal} object executeCommand consumes — the signatures do not match). Reuse these pieces verbatim-in-spirit: parseCommand whitespace split (L65-74), buildEnv from DEFAULT_HOOK_ENV_KEYS allowlist (L53-63), spawn(argv[0], argv.slice(1), { cwd, env, stdio, shell:false }) (L118-126), timeout timer + child.kill("SIGKILL") (L144-151), stdout byte cap at HOOK_LIMITS.maxCommandStdoutBytes + kill on overflow (L162-174), stderr byte cap at HOOK_LIMITS.maxCommandStderrBytes (L176-181), finalize() with timedOut/aborted/stdout-cap/JSON.parse/validateHookOutput (L183-236), child.on('error') ENOENT -> synthesized failure (L238-254), child.on('close') -> finalize (L256-262), stdin write of stdinPayload (L264-269). Import validateHookOutput + HOOK_LIMITS + DEFAULT_HOOK_ENV_KEYS from their existing modules (these are stdlib/type-only and worker-safe). LEAVE OUT the trust gate entirely: do NOT import HookCommandTrustService, do NOT call isTrusted or setTrusted — main already decided trust before dispatching the execute-hook IPC (the main-side HookDispatcher gates on HookCommandTrustService.isTrusted before calling hookExecutionClient.execute; the worker's copy would be a separate instance where setTrusted is never called, so importing the gate would make every hook return untrustedResult — a dead path). The helper returns {stdout, stderr, durationMs, result} shaped to populate hookResultEventSchema.

    Create `src/service/hooks/hookExecutionClient.ts` (main-side). Clone the fork + round-trip lifecycle from WorkspaceWatchManager.ts. Lazy singleton: the worker child process is forked on first execute() call and reused (Pitfall 1). `async execute(input: {hookRunId; command; cwd?; envAllowlist?; timeoutMs?; stdinPayload}, abortSignal?: AbortSignal): Promise<CommandHookExecutionResult>` — send execute-hook, await the hook-result with that hookRunId, enforce timeoutMs (kill the child on timeout via a client-side timer that aborts), honor abortSignal. On missing/malformed hook-result or timeout, return a synthesized non-fatal failure result (NEVER throw — the dispatcher/aggregator treats it as a warn-mode hook error). Expose a shutdown() that sends shutdown + force-kills after a short timeout (mirror WorkspaceWatchManager.shutdown). Provide a test seam (constructor accepts an injected fork/spy so the vitest can mock the round-trip without a real fork).

    Build registration: add a worker entry to `forge.config.js` in the build section — `{ entry: "src/childprocess/hook-execution/HookExecutionWorker.ts", config: "vite.hookExecutionWorker.config.mjs" }` (clone the aifetchly-config entry shape at lines 401-411). Create `vite.hookExecutionWorker.config.mjs` by cloning `vite.aifetchlyConfigWorker.config.mjs` verbatim and swapping the entry/output names to the hook-execution worker.

    <!-- planner-discipline-allow: spawn -->
    Note: the worker's child-execution primitive is the existing shell-disabled API (inherited from CommandHookExecutor, shell:false) — do NOT introduce shell-mode execution anywhere (HOK-02 + injection risk). The SC2 grep gate asserts the DISPATCHER file is clean, not CommandHookExecutor (which legitimately retains its usage, now adapted inside the worker).

    Auto-commit per file (protocol, worker entry + spawn-core helper, client, build config).
  </action>
  <verify>
    <automated>AIFETCHLY_SKIP_TSC=1 npx vitest run --config vite.utilityCode.config.mjs test/vitest/utilitycode/hooks/HookExecutionWorker.test.ts -x && AIFETCHLY_SKIP_TSC=1 npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - HookExecutionWorker.test.ts green for: valid execute-hook -> hook-result round-trip (mocked fork); spawn-core timeout -> hook-result.error.timedOut; malformed inbound message dropped (no crash); strict() rejects smuggled fields.
    - hookExecutionClient lazy singleton: the fork happens at most once across multiple execute() calls (assert via the injected spy); timeout/abort synthesize a non-fatal failure (no throw).
    - WAT-02 (worker-no-DB): the following returns NOTHING — `grep -rn "typeorm\|better-sqlite3\|SqliteDb\|@/modules\|@/model\|electron" src/childprocess/hook-execution/` (the worker dir imports none of these).
    - Worker spawn-core has NO trust gate: `grep -rn "HookCommandTrustService\|isTrusted\|setTrusted" src/childprocess/hook-execution/` returns NOTHING (trust decided in main before dispatch — the worker never imports the trust service).
    - Worker spawn-core does NOT import executeCommand whole: `grep -rn "executeCommand\|from.*CommandHookExecutor" src/childprocess/hook-execution/` returns NOTHING (the spawn-core is a worker-local adaptation, not an import).
    - Worker reuses shell-disabled execution: the child-execution path uses shell:false (verify `shell: false` is present in the spawn-core helper, no `shell: true` anywhere in the worker dir).
    - Build registration: `grep -n "hook-execution/HookExecutionWorker" forge.config.js` returns 1 match and `test -f vite.hookExecutionWorker.config.mjs` succeeds.
    - `npx tsc --noEmit` 0 errors.
  </acceptance_criteria>
  <done>
    The hook-execution worker fork entry (with a worker-local spawn-core helper adapted from CommandHookExecutor L76-271, no trust gate), zod protocol, main client (lazy singleton, hookRunId correlation, non-fatal failure synthesis), and forge/vite build registration are in place; the worker is DB/Electron/trust-service-free (WAT-02); config-sourced command execution is ready to be routed here by the dispatcher in Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HookDispatcher command-hook -> worker round-trip + skill-ref no-op diagnostic + SessionStart/Stop emitters (A5) + HOK-02/TRS-05/SC2 gates</name>
  <files>
    src/service/hooks/HookDispatcher.ts,
    src/service/StreamEventProcessor.ts,
    test/vitest/utilitycode/hooks/HookDispatcher.test.ts
  </files>
  <read_first>
    - src/service/hooks/HookDispatcher.ts (FULL — executeHooks dispatch loop; find the branch that handles hook.type === 'command' and currently invokes the executor in-process; this is the branch you reroute through hookExecutionClient. Also find where hook.type === 'callback' invokes CallbackHookExecutor — leave that path in-process per HOK-02, which constrains only config-sourced COMMAND hooks)
    - src/service/hooks/executors/CommandHookExecutor.ts (the in-process execution path being replaced by the worker round-trip for command hooks; the main-side trust gate HookCommandTrustService.isTrusted STAYS here in main — the dispatcher checks trust BEFORE calling hookExecutionClient.execute, so the worker never needs to)
    - src/service/hooks/HookResultAggregator.ts (deny-wins, continue:false->blocked, non-fatal hookErrors — unchanged; the dispatcher feeds it. Verify a synthesized non-fatal failure from the client flows as a warn-mode error here)
    - src/service/hooks/builtinHooks.ts lines 25-38 (the in-repo PreToolUse DENY precedent: continue:false + reason — config-sourced command hooks deny by writing JSON to stdout, the aggregator converts it, StreamEventProcessor reads blocked. Phase 17 FEEDS this path; do not rewrite it)
    - src/service/StreamEventProcessor.ts (the PreToolUse/PostToolUse emit sites around L752/L781/L821 — the pattern to clone for SessionStart at stream-start and Stop at stream-completion; also L494-506 where blocked/permissionDecision=deny short-circuits to buildHookBlockedToolResult — unchanged)
    - src/service/hooks/hookExecutionClient.ts (Task 1 output — the client you call from the dispatcher)
    - src/service/aifetchlyConfig/AIFetchlyConfigConstants.ts (Plan 01 added the skill-registry-not-available code — emit it for skill-ref hooks)
    - src/entityTypes/hookTypes.ts (HookDefinition type discriminator: command vs callback; the skill-ref marker — a command hook whose declared action was a skill reference, identifiable via the definition built in Plan 02)
    - .planning/phases/17-hooks/17-RESEARCH.md (Dispatcher branching recommendation: callback -> in-process CallbackHookExecutor; command -> worker round-trip; skill -> no-op + diagnostic. A5 resolution: wire SessionStart/Stop emitters at stream start/stop. Open Q1 RESOLVED: wire this phase)
    - .planning/phases/17-hooks/17-VALIDATION.md (HOK-02 no-main-shell + non-fatal + unsupported-event + skill-ref rows; SC2/TRS-05/WAT-02 grep gates)
  </read_first>
  <behavior>
    - Command-hook routing (HOK-02 SC2): a matched command hook causes the dispatcher to call hookExecutionClient.execute (worker round-trip); the dispatcher file performs NO in-process child execution. The result (stdout JSON) flows into HookResultAggregator unchanged.
    - Callback hooks unchanged: hook.type === 'callback' still invokes CallbackHookExecutor in-process (HOK-02 constrains only config-sourced command hooks; built-in callback hooks stay trusted + in-process).
    - Skill-ref no-op (D-Vocabulary / HOK-02 SC4): a hook whose declared action was a skill reference (built by Plan 02 with the placeholder command) emits a non-fatal skill-registry-not-available diagnostic at fire time and performs NO execution; it does NOT throw and does NOT block the stream.
    - Non-fatal failures (HOK-02 SC4): worker timeout / malformed result / worker error surface as a warn-mode aggregator error (hookErrors), never throw into the stream; the tool call proceeds (or is denied only if a DIFFERENT hook denies).
    - PreToolUse DENY preserved: a command hook that writes {continue:false, reason} still produces a DENY via the aggregator -> StreamEventProcessor buildHookBlockedToolResult path (unchanged end-to-end; just routed through the worker).
    - SessionStart/Stop live (A5): StreamEventProcessor emits SessionStart at stream start and Stop at stream completion via HookDispatcher.executeHooks, mirroring the existing PreToolUse emit; hooks registered for those events now fire.
    - No-hooks fast path preserved: when getMatchingHooks is empty, the dispatcher returns EMPTY_AGGREGATE before any IPC (Pitfall 1 — the worker is not contacted).
  </behavior>
  <action>
    HookDispatcher: reroute the command-hook branch. Today the command branch invokes CommandHookExecutor in-process; replace that invocation with a call to `hookExecutionClient.execute(...)` (the Task 1 client), mapping the hook + input + abortSignal to the client's execute() payload (command from hook.command, cwd/envAllowlist/timeoutMs from the hook, stdinPayload = JSON.stringify(input)). The main-side trust gate (HookCommandTrustService.isTrusted) STAYS in the dispatcher BEFORE the client call — main decides trust, the worker just executes what main trusted (per Task 1 mandate the worker has no trust gate). Inject the client via the dispatcher's constructor (test seam) so the dispatcher test can mock the round-trip. The returned CommandHookExecutionResult feeds the SAME downstream path (JSON.parse stdout -> validateHookOutput -> aggregator) — do NOT change the aggregator or the validator. Keep the callback branch (CallbackHookExecutor) in-process and unchanged. Add the skill-ref branch: when a hook is marked as a skill reference (the definition shape built by Plan 02 — detect it the same way Plan 02 marks it), emit a non-fatal skill-registry-not-available diagnostic (use the Plan 01 code) and skip execution, returning an empty/warn result so the aggregator treats it as a documented no-op. Do NOT perform in-process child execution anywhere in the dispatcher.

    StreamEventProcessor: add two emit sites mirroring the existing PreToolUse executeHooks call — SessionStart at the beginning of stream processing (before the first tool call) and Stop at graceful stream completion. Clone the existing executeHooks invocation shape (build the hook input, call HookDispatcher.executeHooks, feed results to the aggregator if a deny is possible — though SessionStart/Stop are observe+inject only per D-Blocking, they cannot deny). If no clean stream-start/stream-completion boundary exists for Stop, emit Stop in the stream finally/shutdown path; document the chosen site. A5 resolved: all four HOK-01 events are now live (PreToolUse + PostToolUse already; SessionStart + Stop added here). Do NOT change the PreToolUse deny short-circuit at L494-506 (buildHookBlockedToolResult).

    Leave HookResultAggregator, HookOutputValidator, HookMatcher, HookCommandTrustService (main-side trust gate), and the builtinHooks DENY precedent UNCHANGED — this plan feeds the existing runtime, it does not rewrite it.

    TRS-05 note: if any hook list/diagnostic IPC channel is added (optional), it MUST use registerValidatedHandler (non-AI-serving), NOT the AI-gated variant. Prefer not adding IPC this plan unless HOK-02 requires it.

    Auto-commit per concern (dispatcher routing, skill-ref no-op, SessionStart/Stop emitters).
  </action>
  <verify>
    <automated>AIFETCHLY_SKIP_TSC=1 npx vitest run --config vite.utilityCode.config.mjs test/vitest/utilitycode/hooks/HookDispatcher.test.ts -x && AIFETCHLY_SKIP_TSC=1 npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - HookDispatcher.test.ts green for: command hook -> client.execute called (mocked) -> result flows to aggregator; callback hook -> still in-process (client NOT called); skill-ref hook -> skill-registry-not-available diagnostic emitted, no execution, no throw; worker timeout/malformed -> warn-mode aggregator error, stream continues; PreToolUse deny via worker round-trip still produces blocked (DENY) end-to-end; no-hooks fast path returns EMPTY_AGGREGATE without contacting the client.
    - SC2 (no main shell, region-scoped): `grep -n "spawn" src/service/hooks/HookDispatcher.ts` returns NOTHING (the dispatcher performs no in-process child execution). NOTE: CommandHookExecutor.ts legitimately retains its usage because its spawn-core is now adapted INSIDE the worker — the SC2 gate is region-scoped to the dispatcher, not the whole hooks dir.
    - TRS-05 (AI gating): if any hook IPC file is created in src/main-process/communication/, `grep -c "registerAiValidatedHandler" src/main-process/communication/*hook*` returns 0 (hook channels use registerValidatedHandler). If no hook IPC is added, this gate is vacuously satisfied — document that.
    - SessionStart/Stop wired: `grep -c "SessionStart\|Stop" src/service/StreamEventProcessor.ts` >= 2 (both emit sites present) and they call HookDispatcher.executeHooks.
    - Aggregator/validator unchanged: `git diff src/service/hooks/HookResultAggregator.ts src/service/hooks/HookOutputValidator.ts` returns no functional change (only the dispatcher + StreamEventProcessor are modified in this task).
    - `npx tsc --noEmit` 0 errors.
  </acceptance_criteria>
  <done>
    Config-sourced command hooks route through the worker (no in-process child execution in the dispatcher); callback hooks stay in-process; skill-ref hooks emit skill-registry-not-available and no-op; worker failures are non-fatal; PreToolUse DENY still works end-to-end via the worker round-trip; SessionStart/Stop are live; SC2 + TRS-05 gates green; the existing aggregator/validator/executor-core are reused not rewritten.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main (HookDispatcher) -> worker (HookExecutionWorker) | config-sourced command-hook execution crosses from the trusted main process into the isolated worker child |
| worker -> child command process | the worker executes the author-declared command string (untrusted author input) in a shell-disabled child |
| command stdout -> HookOutputValidator -> aggregator | untrusted hook stdout crosses into the dispatch result (smuggling vector) |

## STRIDE Threat Register (ASVS L1; block-on-high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-10 | Tampering | config-sourced command hook execution (HOK-02 SC2) | high | mitigate | Command hooks route through the worker — the dispatcher performs NO in-process child execution (SC2 grep gate, region-scoped to HookDispatcher.ts). Workspace hooks already gated by trust.hooks (Plan 02). shell:false inherited (no shell metachar expansion). (V12 Files & Resources.) |
| T-17-11 | Elevation | worker process compromise -> DB access (T-worker-compromise) | high | mitigate | Worker is pure-Node fork; WAT-02 grep gate forbids database/ORM/Electron/registry/model/HookCommandTrustService imports in src/childprocess/hook-execution/. The worker-local spawn-core helper imports only validateHookOutput + HOOK_LIMITS + DEFAULT_HOOK_ENV_KEYS (worker-safe) — it does NOT import executeCommand whole or HookCommandTrustService. Trust is decided in main before dispatch. |
| T-17-12 | Elevation | hook stdout smuggling {permissionDecision:'allow'} to bypass SkillExecutor | high | mitigate | HookOutputValidator parses + enum-validates stdout; HookResultAggregator's "allow is advisory" rule means hook allow NEVER bypasses SkillExecutor/SkillPermissionService (StreamEventProcessor runs SkillExecutor regardless). Reused unchanged. (V4 Access Control.) |
| T-17-13 | Information Disclosure | command hook exfiltrates secrets via env | medium | mitigate | DEFAULT_HOOK_ENV_KEYS allowlist (PATH/HOME/USER/USERNAME/TEMP/TMP only) inherited from CommandHookExecutor; process.env never spread; Token secrets never injected. (V12.) |
| T-17-14 | Denial of Service | oversized stdout / matcher / worker latency (Pitfall 1/3) | medium | mitigate | stdout capped at maxCommandStdoutBytes (256KB), matcher at 128 chars (inherited); worker is a lazy long-lived singleton (one IPC round-trip per firing, not a fork); no-hooks fast path returns EMPTY_AGGREGATE before any IPC. |
| T-17-15 | Denial of Service | workspace PreToolUse deny-all hook gates every tool call | medium | mitigate | Workspace deny hooks gated behind trust.hooks (D-Blocking); user must opt in; HookCommandTrustService defense-in-depth (main-side). |
| T-17-16 | Tampering | skill-ref hook (D-Vocabulary) | low | mitigate | Registers but emits skill-registry-not-available diagnostic at fire time and performs NO execution — no untrusted code path until Phase 18 wires a real skill registry. |
| T-17-17 | Denial of Service | malformed worker message / timeout crashes the AI stream | high | mitigate | Both protocol directions safeParse'd (strict); malformed inbound dropped (worker) / synthesized non-fatal failure (client); HookResultAggregator makes failures non-fatal (warn mode). The stream NEVER crashes from a hook failure (HOK-02 SC4). |
| T-17-SC | Tampering | package installs | n/a | accept | Zero new packages this plan (RESEARCH § Package Legitimacy Audit). |

</threat_model>

<verification>
- `AIFETCHLY_SKIP_TSC=1 npx vitest run --config vite.utilityCode.config.mjs test/vitest/utilitycode/hooks/HookExecutionWorker.test.ts test/vitest/utilitycode/hooks/HookDispatcher.test.ts -x` green.
- `npx tsc --noEmit` 0 errors.
- SC2 (region-scoped): `grep -n "spawn" src/service/hooks/HookDispatcher.ts` empty.
- WAT-02: `grep -rn "typeorm\|better-sqlite3\|SqliteDb\|@/modules\|@/model\|electron" src/childprocess/hook-execution/` empty.
- Worker no-trust-gate: `grep -rn "HookCommandTrustService\|isTrusted\|setTrusted" src/childprocess/hook-execution/` empty.
- TRS-05: ZERO AI-gated handlers for any hook channel (document vacuous-satisfaction if no hook IPC added).
- Aggregator/validator/executor-core reused unchanged (git diff confirms no rewrite of the dispatch loop).
- Do NOT run bare `yarn testmain` (RESEARCH Pitfall 6).
</verification>

<success_criteria>
- HOK-02 SC2: config-sourced command hooks never execute in main (dispatcher routes through the worker; SC2 grep green).
- HOK-02 worker: the worker runs a worker-local spawn-core helper adapted from CommandHookExecutor (shell:false, env allowlist, timeout, caps, validateHookOutput) and is DB/Electron/trust-service-free (WAT-02); trust is decided in main.
- HOK-02 SC4: failures/timeouts/unsupported-events/skill-refs are non-fatal diagnostics; the stream never crashes.
- A5 resolved: SessionStart/Stop emitters are live; all four HOK-01 events now fire.
- The existing dispatcher/aggregator/executor loop is FED, not rewritten.
</success_criteria>

<output>
Create `.planning/phases/17-hooks/17-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 03 contribution)

| Artifact | Kind | File | Purpose |
|----------|------|------|---------|
| `workerCommandSchema` / `workerEventSchema` | zod protocol | src/childprocess/hook-execution/workerProtocol.ts | validate execute-hook / hook-result both directions (WAT-06) |
| `HookExecutionWorker` (+ `initializeWorker` + worker-local spawn-core helper) | fork entry | src/childprocess/hook-execution/HookExecutionWorker.ts | runs a spawn-core adapted from CommandHookExecutor L76-271 in the isolated worker (no trust gate) |
| `hookExecutionClient` | main IPC client | src/service/hooks/hookExecutionClient.ts | lazy singleton fork + hookRunId correlation + non-fatal synthesis |
| forge worker entry + `vite.hookExecutionWorker.config.mjs` | build config | forge.config.js / vite.hookExecutionWorker.config.mjs | bundle the worker so it can be forked at runtime |
| HookDispatcher command-hook -> worker branch + skill-ref no-op branch | dispatch routing | src/service/hooks/HookDispatcher.ts | HOK-02 SC2 no-main-shell + D-Vocabulary skill no-op |
| SessionStart/Stop emit sites | event wiring | src/service/StreamEventProcessor.ts | A5 — completes HOK-01 event coverage |

## Assumptions / open items resolved this plan
- A4 (long-lived worker): lazy singleton forked on first command-hook dispatch, reused thereafter (Pitfall 1 mitigation).
- A5 (SessionStart/Stop emitters): WIRED this plan (two executeHooks calls in StreamEventProcessor at stream start/completion) — per RESEARCH Open Q1 RESOLVED recommendation (cheap, completes HOK-01 coverage). The parser (Plan 02) already accepts all four events; this plan makes SessionStart/Stop live.
- Open Q1 (SessionStart/Stop wire or defer): RESOLVED — wire.
- Pitfall 1 (worker spawn latency): lazy long-lived singleton + preserved no-hooks fast path.
- Pitfall 3 (shell-disabled splitter limitation): inherited unchanged from CommandHookExecutor; documented for hook authors (commands run shell-disabled; wrap complex commands in a script file).
- RESEARCH execution-boundary decision (Option (b)): implemented — new dedicated worker; scan-worker reuse rejected (lifecycle + WAT-02); in-process allowlist eliminated (violates SC2). The spawn-core is a worker-local adaptation of CommandHookExecutor L76-271 (NOT a whole import) because executeCommand gates on a main-process trust decision (HookCommandTrustService.isTrusted L83) whose worker-side instance would always be untrusted, and its signature does not match the IPC protocol.
