---
phase: 18-skills-and-plugin-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/service/aifetchlyConfig/AIFetchlyConfigConstants.ts
  - src/service/aifetchlyConfig/buildLocalSkillDraft.ts
  - src/service/aifetchlyConfig/AIFetchlyConfigLoader.ts
  - src/service/workspaceWatch/WorkspaceConfigScanner.ts
  - src/service/workspaceWatch/buildWorkspaceSkillDefinitions.ts
  - src/service/LocalSkillSourceAdapter.ts
  - src/service/aifetchlyConfig/AIFetchlyRuntimeRegistrySync.ts
  - test/vitest/main/service/buildLocalSkillDraft.test.ts
  - test/vitest/main/service/AIFetchlyConfigLoader.skills.test.ts
  - test/vitest/utilitycode/workspaceWatch/WorkspaceConfigScanner.skills.test.ts
  - test/vitest/main/service/LocalSkillSourceAdapter.test.ts
  - test/vitest/main/service/AIFetchlyRuntimeRegistrySync.skills.test.ts
  - test/vitest/main/service/SkillImportService.local.test.ts
  - test/vitest/main/service/SkillPermissionService.local.test.ts
autonomous: true
requirements: [SKL-01]
user_setup: []

must_haves:
  truths:
    # SKL-01 success criterion 1 (ROADMAP §Phase 18 SC1)
    - "A manifest at ~/.aifetchly/skills/<name>/manifest.json is read, validated, and turned into a LocalSkillDraft that flows into the existing SkillImportService.registerImportedSkill pipeline"
    - "A registered local skill appears in SkillRegistry.getAllToolFunctions() output (consumed by AIChatQueryEngine at lines 251/580) — i.e. exposed as an OpenAI tool schema automatically, with no new mapping code"
    - "Local skill execution routes through SkillWorkerClient (JS) / PythonSkillRuntimeService (Python) — the existing utility-process boundary — and NEVER loads skill entry code on the Electron main thread"
    - "SkillPermissionService.checkPermission fires per local-skill execution call (D-SkillEnable gate-at-call); manifest permissions feed the gate exactly as zip-installed skills do"
    - "A local skill whose name collides with a built-in skill produces a non-fatal manifest-invalid diagnostic and does NOT crash the app (built-in names always win)"
    - "Deleting ~/.aifetchly/skills/<name>/ and rescanning unregisters the skill (no stale entries survive a rescan)"
    - "Untrusted-workspace skills are dropped by the skills: trust-filter line BEFORE the registry is mutated (mirror of the Phase 17 hooks: line)"
    - "The workspace scanner stays scan-only: zero database, Electron, SkillRegistry, CommandRegistry, or AgentDefinitionRegistry imports in WorkspaceConfigScanner.ts (WAT-02 boundary holds)"
  artifacts:
    - "src/service/aifetchlyConfig/buildLocalSkillDraft.ts — pure validator producing { ok: true, draft } | { ok: false, diagnostic }"
    - "src/service/LocalSkillSourceAdapter.ts — tracks sourceId -> Set<skillName>, performs unregister-then-register reconciliation"
    - "src/service/workspaceWatch/buildWorkspaceSkillDefinitions.ts — main-side raw-draft -> LocalSkillDraft converter"
    - "tryReadSkillFiles methods on both AIFetchlyConfigLoader (global) and WorkspaceConfigScanner (worker raw-draft)"
    - "skills: trust-filter line in AIFetchlyRuntimeRegistrySync.applyWorkspaceSnapshot"
    - "registerLocalSkills call block in AIFetchlyRuntimeRegistrySync.applySnapshot + skills clear in removeSource"
    - "AIFETCHLY_CONFIG_LIMITS.skillManifestBytes + maxSkillsPerSource constants + manifest-invalid diagnostic code"
  key_links:
    - "AIFetchlyConfigLoader.tryReadSkillFiles -> snapshot.skills (source=user) -> AIFetchlyRuntimeRegistrySync.applySnapshot -> LocalSkillSourceAdapter -> SkillImportService.registerImportedSkill -> SkillRegistry.registerSkill"
    - "WorkspaceConfigScanner.tryReadSkillFiles (worker) -> snapshot.skills raw drafts -> applyWorkspaceSnapshot trust.skills gate -> buildWorkspaceSkillDefinitions -> LocalSkillSourceAdapter"
    - "SkillRegistry.registerSkill -> getAllToolFunctions -> skillDefinitionToToolFunction -> AIChatQueryEngine (automatic, no new code)"
    - "SkillExecutor.execute -> SkillPermissionService.checkPermission -> skill.execute -> SkillWorkerClient.execute (existing boundary, unchanged)"
---

<objective>
Register local skills discovered under `~/.aifetchly/skills/<name>/manifest.json` (and trusted-workspace `<ws>/.aifetchly/skills/`) through the EXISTING `SkillRegistry` -> `SkillExecutor` -> `SkillPermissionService` pipeline, and add the `skills:` trust-filter line mirroring Phase 17's `hooks:` line (D-WorkspaceSkills, D-SkillEnable).

This is an INTEGRATION plan, not greenfield. The skill runtime (`SkillRegistry`, `SkillExecutor`, `SkillWorkerClient`, `PythonSkillRuntimeService`, `SkillPermissionService`, `SkillImportService`) ALREADY EXISTS and is unchanged. Phase 18's job is the WIRING: a discovery path (`tryReadSkillFiles`) that feeds validated manifests INTO `SkillImportService.registerImportedSkill` with `skillDir = ~/.aifetchly/skills/<name>`.

Purpose: Satisfy SKL-01 and ROADMAP §Phase 18 SC1. Local skills are validated, registered, exposed as OpenAI tools, executed via the existing sandboxed boundary, and permission-checked — never loaded as arbitrary code into the main process.

Output: `buildLocalSkillDraft.ts` (pure validator), `LocalSkillSourceAdapter.ts` (source-reconciliation adapter — bridges the fact that SkillRegistry has NO `replaceSource`), `buildWorkspaceSkillDefinitions.ts` (converter), `tryReadSkillFiles` on both loaders, the `skills:` trust line, and 6 TDD test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-skills-and-plugin-integration/18-CONTEXT.md
@.planning/phases/18-skills-and-plugin-integration/18-RESEARCH.md
@.planning/phases/18-skills-and-plugin-integration/18-VALIDATION.md

# Phase-17 analog (the template this plan mirrors — hooks discovery + trust filter)
@src/service/aifetchlyConfig/AIFetchlyConfigLoader.ts
@src/service/workspaceWatch/WorkspaceConfigScanner.ts
@src/service/workspaceWatch/buildWorkspaceHookDefinitions.ts
@src/service/aifetchlyConfig/AIFetchlyRuntimeRegistrySync.ts
@src/service/aifetchlyConfig/AIFetchlyConfigConstants.ts
@src/entityTypes/aifetchlyConfigTypes.ts

# Existing skill runtime (UNCHANGED — integration target)
@src/config/skillsRegistry.ts
@src/service/SkillImportService.ts
@src/service/SkillExecutor.ts
@src/service/SkillPermissionService.ts
@src/entityTypes/skillTypes.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure buildLocalSkillDraft validator + skill constants + draft types</name>
  <files>
    src/service/aifetchlyConfig/buildLocalSkillDraft.ts,
    src/service/aifetchlyConfig/AIFetchlyConfigConstants.ts,
    src/entityTypes/aifetchlyConfigTypes.ts,
    test/vitest/main/service/buildLocalSkillDraft.test.ts
  </files>
  <read_first>
    - src/service/SkillImportService.ts (lines 572-681: validateManifest — the EXISTING manifest schema owner; confirm whether it is static/instance and whether importing the full SkillImportService pulls zip/DB concerns — per 18-RESEARCH Pattern 1, factor validateManifest + resolvePermissionCategory into a pure callable if the full service import is undesirable)
    - src/entityTypes/skillTypes.ts (lines 289-335: SkillManifest interface — the EXISTING manifest shape this plan REUSES; do NOT invent a new format)
    - src/service/aifetchlyConfig/AIFetchlyConfigConstants.ts (AIFETCHLY_CONFIG_LIMITS at line 30; AIFETCHLY_DIAGNOSTIC_CODES at line 91; skill-registry-not-available already at line 113)
    - src/service/workspaceWatch/buildWorkspaceHookDefinitions.ts (the Phase-17 converter pattern: pure function, sourceMeta, returns { definitions, diagnostics })
    - src/service/hooks/buildHookDefinition.ts or the Phase-17 buildHookDefinition (the single-owner validator pattern: { ok: true, definition } | { ok: false, diagnostic })
    - src/entityTypes/aifetchlyConfigTypes.ts (AIFetchlyConfigDiagnostic shape at line 49; AIFetchlyConfigFileKind includes "skill" at line 34 — already reserved)
  </read_first>
  <behavior>
    - Valid manifest (name matches ^[a-z][a-z0-9_-]*$, semver version, description <=500 chars, runtime in {javascript, python}, parameters.type==="object", entry is a relative path with no traversal, optional permissions in {network,filesystem,automation}) with an existing entry file at skillDir/manifest.entry -> { ok: true, draft: { name, manifest, skillDir, contentHash } }
    - Manifest failing the existing SkillManifest schema (bad name, bad semver, runtime outside the set, parameters not type object) -> { ok: false, diagnostic } with code "manifest-invalid" (the NEW code added to AIFETCHLY_DIAGNOSTIC_CODES)
    - Manifest whose entry field contains path traversal (per CFG-05) -> { ok: false, diagnostic } code "manifest-invalid"
    - Count cap: a source exceeding maxSkillsPerSource -> the caller (Task 2 loader) emits one "count-cap" diagnostic and stops (the constant is added here)
  </behavior>
  <action>
    Add two constants to AIFETCHLY_CONFIG_LIMITS in AIFetchlyConfigConstants.ts: `skillManifestBytes: 64 * 1024` (CFG-04 cap per manifest.json, mirroring commandMdBytes) and `maxSkillsPerSource: 100` (CFG-06 cap, mirroring maxAgentsPerSource/maxHooksPerSource). Add the diagnostic code `manifest-invalid` to the AIFETCHLY_DIAGNOSTIC_CODES tuple (closed set — place it adjacent to the Phase 17 hook codes with a Phase-18 comment). Do NOT remove or rename any existing code.

    Create `src/service/aifetchlyConfig/buildLocalSkillDraft.ts` as a PURE leaf (no Electron, no DB, no SkillRegistry imports — only type imports from skillTypes.ts and aifetchlyConfigTypes.ts). It mirrors the Phase-17 buildHookDefinition single-owner pattern. Define the LocalSkillDraft interface here: `{ readonly name: string; readonly manifest: SkillManifest; readonly skillDir: string; readonly contentHash: string }`. The function signature: `buildLocalSkillDraft(raw: unknown, sourceMeta: { source: "user" | "workspace"; sourceId: string; relativePath: string }, skillDir: string, contentHash: string): { ok: true; draft: LocalSkillDraft } | { ok: false; diagnostic: AIFetchlyConfigDiagnostic }`.

    Validation MUST delegate to the EXISTING SkillImportService.validateManifest (the single schema owner — do NOT re-implement name regex, semver, runtime, python-block, or path-traversal checks). Per 18-RESEARCH Pattern 1 / Discretion Item 1, if importing the full SkillImportService is undesirable because it pulls zip/DB-bound code into the config loader, factor validateManifest and resolvePermissionCategory into a pure shared helper that both SkillImportService and buildLocalSkillDraft import — but DO NOT duplicate the validation rules. Wrap validateManifest's result: success -> construct the draft; failure -> map to a manifest-invalid diagnostic carrying the validation message. CFG-05 path safety on the entry field is already inside validateManifest (verified 18-RESEARCH Pattern 1) — do not re-check.

    Also define `WorkspaceSkillDraft` (the worker raw-draft shape: `{ readonly name: string; readonly rawManifest: unknown; readonly skillDir: string; readonly contentHash: string; readonly relativePath: string }`) — export it from this file or from aifetchlyConfigTypes.ts so the worker scanner (Task 2) can import it without pulling main-only code. The rawManifest is an unvalidated `unknown` blob (worker stays scan-only per WAT-02; validation happens main-side in Task 2's converter).

    Per D-SkillEnable: no "enable" flag is introduced here — registration is immediate, gating is at call time via the existing SkillPermissionService.
  </action>
  <verify>
    <automated>AIFETCHLY_SKIP_TSC=1 npx vitest run --config vite.main.config.mjs test/vitest/main/service/buildLocalSkillDraft.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - buildLocalSkillDraft.test.ts exits 0 (RED first — write tests before the implementation, confirm they fail, then implement to green)
    - `grep -c "manifest-invalid" src/service/aifetchlyConfig/AIFetchlyConfigConstants.ts` returns >= 1 (new code added to the closed set)
    - `grep -c "skillManifestBytes\|maxSkillsPerSource" src/service/aifetchlyConfig/AIFetchlyConfigConstants.ts` returns >= 2
    - buildLocalSkillDraft.ts has ZERO imports from electron, better-sqlite3, TypeORM, or SkillRegistry: `grep -E "electron|better-sqlite3|typeorm|skillsRegistry" src/service/aifetchlyConfig/buildLocalSkillDraft.ts && exit 1 || exit 0` exits 0
    - The SkillManifest type is reused (not redefined): `grep -c "interface SkillManifest" src/service/aifetchlyConfig/buildLocalSkillDraft.ts` returns 0 (it imports the type)
    - A valid manifest -> { ok: true, draft.manifest } preserves the validated SkillManifest; an entry with traversal -> { ok: false, diagnostic.code === "manifest-invalid" }
  </acceptance_criteria>
  <done>
    buildLocalSkillDraft is a pure validator reusing SkillImportService.validateManifest; LocalSkillDraft + WorkspaceSkillDraft types defined; skillManifestBytes/maxSkillsPerSource/manifest-invalid constants added; all buildLocalSkillDraft.test.ts cases pass RED->GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Discovery wiring — global tryReadSkillFiles + worker raw-draft scan + workspace converter</name>
  <files>
    src/service/aifetchlyConfig/AIFetchlyConfigLoader.ts,
    src/service/workspaceWatch/WorkspaceConfigScanner.ts,
    src/service/workspaceWatch/buildWorkspaceSkillDefinitions.ts,
    test/vitest/main/service/AIFetchlyConfigLoader.skills.test.ts,
    test/vitest/utilitycode/workspaceWatch/WorkspaceConfigScanner.skills.test.ts
  </files>
  <read_first>
    - src/service/aifetchlyConfig/AIFetchlyConfigLoader.ts (tryReadHookFiles at line 641 — the EXACT template for the global tryReadSkillFiles; note how it is called from the scan method around line 257, the source/sourceId literals, the stat->sizeCap->read->hash->pushFileSnapshot->parse->validate->countCap->push pattern)
    - src/service/workspaceWatch/WorkspaceConfigScanner.ts (tryReadHookFiles at line 781 — the worker raw-draft template; WorkspaceHookDraft at line 96; how the scan method assembles agents/hooks drafts around lines 161-162/217-221/299; the readdir-withFileTypes pattern at line 513/651)
    - src/service/workspaceWatch/buildWorkspaceHookDefinitions.ts (the Phase-17 main-side converter: raw WorkspaceHookDraft[] -> validated CommandHookDefinition[] + diagnostics; this is the template for buildWorkspaceSkillDefinitions)
    - src/service/aifetchlyConfig/buildLocalSkillDraft.ts (Task 1 output — the validator this task calls)
    - src/service/aifetchlyConfig/AIFetchlyConfigConstants.ts (AIFETCHLY_CONFIG_DIR_NAME, AIFETCHLY_CONFIG_LIMITS — confirm the SKILLS_DIR constant name; if absent, add "skills" as a dir constant alongside HOOKS_DIR)
  </read_first>
  <behavior>
    - Global loader with ~/.aifetchly/skills/my-skill/manifest.json present and valid -> snapshot.skills contains one LocalSkillDraft, files[] contains a "skill" file snapshot, no diagnostics
    - Global loader with no skills/ dir -> snapshot.skills empty, NO diagnostic (ENOENT is the happy path, mirroring tryReadHookFiles)
    - Global loader with a manifest exceeding skillManifestBytes -> one "file-too-large" diagnostic, skill skipped
    - Global loader exceeding maxSkillsPerSource -> one "count-cap" diagnostic, surplus skills dropped
    - Global loader with an invalid manifest -> one "manifest-invalid" diagnostic, other valid skills still registered
    - Worker scanner with <ws>/.aifetchly/skills/foo/manifest.json -> raw WorkspaceSkillDraft pushed (unvalidated unknown blob); ZERO validation, ZERO registry calls (WAT-02)
    - Worker scanner reads ONLY manifest.json per skill dir; it does NOT read the entry .js/.py file content (main process reads entry at registration time per 18-RESEARCH Pattern 9 / Anti-Pattern)
    - buildWorkspaceSkillDefinitions converts raw WorkspaceSkillDraft[] -> LocalSkillDraft[] main-side via buildLocalSkillDraft, emitting manifest-invalid diagnostics for failures
  </behavior>
  <action>
    Add a `tryReadSkillFiles` private async method to AIFetchlyConfigLoader mirroring `tryReadHookFiles` (line 641), adapted for DIRECTORIES instead of a single file: (1) `fs.promises.readdir(path.join(this.rootPath, SKILLS_DIR), { withFileTypes: true })` — ENOENT is the happy path (return, no diagnostic); ioError otherwise. (2) For each directory entry (skip non-directories): check `skills.length >= maxSkillsPerSource` -> push one count-cap diagnostic and break; stat `<skillsDir>/<name>/manifest.json` (CFG-04 skillManifestBytes cap -> file-too-large); readFile; sha256 contentHash; push a file snapshot with kind "skill"; JSON.parse (failure -> manifest-invalid); call buildLocalSkillDraft(parsed, sourceMeta, skillDir, contentHash) where skillDir is the absolute `<rootPath>/skills/<name>`; push draft on ok, push diagnostic on !ok. (3) Wire the call into the scan method alongside the existing `await this.tryReadHookFiles(files, hooks, diagnostics)` call (around line 257) — pass the skills array and accumulate into the snapshot.skills field with source "user". The skillDir MUST be resolved from this.rootPath (the config rootPath), NEVER from SkillEnvironmentManager.getInstalledSkillRoot (Anti-Pattern per 18-RESEARCH).

    Add a `tryReadSkillFiles` private async method to WorkspaceConfigScanner mirroring its `tryReadHookFiles` (line 781), adapted for directories: readdir skills/, per-dir stat+read manifest.json ONLY (do NOT read the entry .js/.py — the main process reads it), JSON.parse, push ONE raw WorkspaceSkillDraft { name, rawManifest: parsed (unknown), skillDir, contentHash, relativePath } — NO validation, NO DB, NO registry. Apply the same skillManifestBytes + maxSkillsPerSource caps. Wire into the scanner's scan method alongside the existing hooks draft assembly (around lines 161-162/217-221/299) so snapshot.skills carries the raw drafts. The skillDir here is the WORKSPACE rootPath + /skills/<name> (the main process resolves the absolute path from the trusted workspace root).

    Create `src/service/workspaceWatch/buildWorkspaceSkillDefinitions.ts` mirroring buildWorkspaceHookDefinitions.ts: a pure main-side function `buildWorkspaceSkillDefinitions(drafts: readonly WorkspaceSkillDraft[], workspaceId: string): { definitions: LocalSkillDraft[]; diagnostics: AIFetchlyConfigDiagnostic[] }` that calls buildLocalSkillDraft for each raw draft (source "workspace", sourceId `workspace:<workspaceId>`), collecting ok drafts and !ok diagnostics. This runs in the MAIN process (it imports buildLocalSkillDraft which delegates to validateManifest) — NOT in the worker.

    CRITICAL worker boundary (WAT-02 / CLAUDE.md): WorkspaceConfigScanner.ts MUST NOT import buildLocalSkillDraft, SkillRegistry, SkillImportService, SqliteDb, TypeORM, or anything electron-bound. The scanner ships RAW drafts only; validation + registration happen main-side. If a constant (SKILLS_DIR, limits) is needed in the worker, import it from AIFetchlyConfigConstants (pure data, no side effects).
  </action>
  <verify>
    <automated>AIFETCHLY_SKIP_TSC=1 npx vitest run --config vite.main.config.mjs test/vitest/main/service/AIFetchlyConfigLoader.skills.test.ts -x && AIFETCHLY_SKIP_TSC=1 npx vitest run --config vite.utilityCode.config.mjs test/vitest/utilitycode/workspaceWatch/WorkspaceConfigScanner.skills.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - Both test files exit 0 (RED->GREEN)
    - WAT-02 grep gate passes (worker stays scan-only): `grep -E "SqliteDb|TypeORM|SkillRegistry|CommandRegistry|AgentDefinitionRegistry|electron" src/service/workspaceWatch/WorkspaceConfigScanner.ts && exit 1 || exit 0` exits 0
    - The worker scanner does NOT import buildLocalSkillDraft: `grep -c "buildLocalSkillDraft" src/service/workspaceWatch/WorkspaceConfigScanner.ts` returns 0
    - buildWorkspaceSkillDefinitions.ts imports buildLocalSkillDraft and returns { definitions, diagnostics }: `grep -c "buildLocalSkillDraft" src/service/workspaceWatch/buildWorkspaceSkillDefinitions.ts` returns >= 1
    - Global loader: a valid ~/.aifetchly/skills/<name>/manifest.json produces snapshot.skills with length 1; missing skills/ dir produces length 0 with zero diagnostics
    - An invalid manifest produces exactly one manifest-invalid diagnostic and does not abort scanning of sibling skills
  </acceptance_criteria>
  <done>
    Global + worker tryReadSkillFiles methods mirror the Phase-17 tryReadHookFiles pattern (adapted for skill directories); buildWorkspaceSkillDefinitions converts raw drafts main-side; WAT-02 grep gate green; all discovery tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: LocalSkillSourceAdapter + skills trust-filter line + sync wiring + execution/permission boundary contract tests</name>
  <files>
    src/service/LocalSkillSourceAdapter.ts,
    src/service/aifetchlyConfig/AIFetchlyRuntimeRegistrySync.ts,
    test/vitest/main/service/LocalSkillSourceAdapter.test.ts,
    test/vitest/main/service/AIFetchlyRuntimeRegistrySync.skills.test.ts,
    test/vitest/main/service/SkillImportService.local.test.ts,
    test/vitest/main/service/SkillPermissionService.local.test.ts
  </files>
  <read_first>
    - src/config/skillsRegistry.ts (registerSkill throws on duplicate name at line 2108; unregisterSkill; isRegistered; getAllToolFunctions at line 2049; listBuiltInSkillDefinitions if present — built-in names always win)
    - src/service/SkillImportService.ts (registerImportedSkill at lines 985-1042 — THE registration entry point this adapter calls; buildImportedSkillExecuteHandler at 1107-1281 routes to SkillWorkerClient at 1276 for JS and PythonSkillRuntimeService for Python; confirm static vs instance method)
    - src/service/SkillExecutor.ts (execute at line 218 — validate -> sanitize -> SkillPermissionService.checkPermission -> skill.execute; this is the path D-SkillRefResolve in Plan 18-02 will call)
    - src/service/SkillPermissionService.ts (checkPermission at line 109 — pure auto-allowed, shell always-prompt, others check Token store; the D-SkillEnable gate, unchanged)
    - src/service/aifetchlyConfig/AIFetchlyRuntimeRegistrySync.ts (applySnapshot at line 92; the hooks block at lines 130-142 — the EXACT template for the skills block; applyWorkspaceSnapshot trust filter at lines 182-197 — the hooks: line at 196 is the template for skills:; removeSource at line 206 — add a skills clear)
    - src/service/slashCommands/CommandRegistry.ts (replaceSource at line 83 — the semantic this adapter emulates for SkillRegistry which has NO replaceSource)
    - src/service/aifetchlyConfig/buildLocalSkillDraft.ts + src/service/workspaceWatch/buildWorkspaceSkillDefinitions.ts (Task 1 + Task 2 outputs)
  </read_first>
  <behavior>
    - LocalSkillSourceAdapter.replaceSource(sourceId, drafts): unregisters every previously-tracked name for sourceId, then registers the new set via SkillImportService.registerImportedSkill; tracks the new names; returns diagnostics for failures
    - A draft whose name collides with an already-registered (built-in) skill -> registerSkill throws -> adapter catches, emits a manifest-invalid diagnostic, continues with the next draft (built-in names ALWAYS win; T-spoof-builtin)
    - Rescan with a removed skill -> the old name is unregistered before the new set registers (no stale entries; T-spoof-builtin / Pitfall 4)
    - applyWorkspaceSnapshot with trust.skills === false -> snapshot.skills forced to [] BEFORE applySnapshot (mirror hooks: line; TRS-01 / T-untrusted-workspace)
    - applyWorkspaceSnapshot with trust.skills === true -> workspace drafts converted via buildWorkspaceSkillDefinitions then registered through the adapter
    - applySnapshot registers global (source "user") skills through the adapter
    - removeSource(sourceId) clears the source's skills (adapter.replaceSource(sourceId, []))
    - SkillImportService.local.test.ts: registering a local JS skill via registerImportedSkill produces a SkillDefinition whose execute handler routes through SkillWorkerClient (mocked) — it does NOT load the entry code on the main thread (T-arbitrary-exec)
    - SkillPermissionService.local.test.ts: SkillExecutor.execute on a registered local skill calls SkillPermissionService.checkPermission(skillName) before invoking skill.execute (T-exfil-args / D-SkillEnable gate-at-call)
  </behavior>
  <action>
    Create `src/service/LocalSkillSourceAdapter.ts` — the adapter that gives SkillRegistry source-replacement semantics WITHOUT rewriting it (18-RESEARCH Pattern 8 / Finding #6: SkillRegistry has registerSkill that throws on duplicate + unregisterSkill, but NO replaceSource; CONTEXT.md locks "do not rewrite SkillRegistry"). The adapter holds a private `Map<string, Set<string>>` (sourceId -> registered skill names). Method `replaceSource(sourceId: string, drafts: readonly LocalSkillDraft[]): AIFetchlyConfigDiagnostic[]`: (1) read the old Set for sourceId; (2) for each old name, call SkillRegistry.unregisterSkill(name) (best-effort, guard against already-gone); (3) for each draft, try SkillImportService.registerImportedSkill(draft.manifest, draft.skillDir) — on throw (the duplicate-name collision with a built-in or a concurrently-registered skill), catch and push a manifest-invalid diagnostic ("skill name collides with an existing skill: <name>"); on success add the name to the next Set; (4) set the index to the next Set. Return the diagnostics array. This mirrors CommandRegistry.replaceSource atomic delete-then-insert semantics (18-RESEARCH Pattern 8).

    Wire skills into AIFetchlyRuntimeRegistrySync.ts: (1) In applyWorkspaceSnapshot (line 182-197), add `skills: trust.skills ? snapshot.skills : []` to the filtered snapshot — directly after the `hooks:` line (line 196), with a Phase-18 TRS-01 comment. (2) In applySnapshot (after the hooks block ending at line 142), add a skills block mirroring the hooks/agents blocks: if snapshot.source === "workspace", cast snapshot.skills to WorkspaceSkillDraft[], derive workspaceId, call buildWorkspaceSkillDefinitions(drafts, workspaceId) -> LocalSkillDraft[]; else cast to LocalSkillDraft[]. Then call the LocalSkillSourceAdapter's replaceSource(snapshot.sourceId, drafts) and surface returned diagnostics. Construct the adapter once (private field or module singleton) — it must be the SAME instance across applySnapshot calls so the sourceId->names index persists across rescans. (3) In removeSource (line 206), add a call to clear the source's skills (adapter.replaceSource(sourceId, [])). (4) Add skillsChanged to the AIFetchlySnapshotApplyResult (it is already in AIFetchlyConfigDiff at aifetchlyConfigTypes.ts line 111; add the field to the result interface and compute it).

    SkillImportService.local.test.ts (contract/characterization test — NO new production code in SkillImportService; 18-RESEARCH Pattern 2/3 confirm the boundary handles local roots with zero changes): register a local JS skill via registerImportedSkill with skillDir pointing at a tmp skill dir; mock SkillWorkerClient.getInstance().execute; invoke the registered SkillDefinition.execute; assert SkillWorkerClient.execute was called with the entry CODE string and args — proving the entry file content is sent to the utility process, NOT loaded on the main thread. Add a Python-runtime variant only if PythonSkillRuntimeService is cheaply mockable (else cover JS only and note Python follows the same buildImportedSkillExecuteHandler branch).

    SkillPermissionService.local.test.ts (contract test — NO new production code in SkillExecutor/SkillPermissionService): register a local skill, then call SkillExecutor.execute(name, args, context); mock or spy on SkillPermissionService.checkPermission; assert it was called with the skill name BEFORE skill.execute ran. Cover the exfil-args mitigation (SkillExecutor.validateArgs rejects sensitive patterns) only if validateArgs is reachable in a unit test without Electron; otherwise assert the checkPermission ordering (T-exfil-args mitigation is owned by existing SkillExecutor code — this test proves it fires for the local path).

    Per D-SkillEnable: NO per-skill enable flag is added — the manifest permissions feed SkillPermissionService exactly as zip-installed skills do. Per TRS-05 Strategy A: if ANY skill-execution IPC is added (none expected this plan — execution is triggered by the existing AI tool-invocation path which is already AI-gated downstream), it MUST use the non-AI-validated handler registrar; never use the AI-gated registrar variant (skill execution IPC is non-AI-serving).
  </action>
  <verify>
    <automated>AIFETCHLY_SKIP_TSC=1 npx vitest run --config vite.main.config.mjs test/vitest/main/service/LocalSkillSourceAdapter.test.ts test/vitest/main/service/AIFetchlyRuntimeRegistrySync.skills.test.ts test/vitest/main/service/SkillImportService.local.test.ts test/vitest/main/service/SkillPermissionService.local.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - All four test files exit 0 (RED->GREEN)
    - LocalSkillSourceAdapter.replaceSource unregisters old source names before registering new ones: a rescan that drops a skill leaves SkillRegistry.isRegistered(droppedName) === false
    - Built-in name collision: registering a local draft whose name matches an existing skill produces a manifest-invalid diagnostic and does NOT throw out of the adapter (T-spoof-builtin)
    - applyWorkspaceSnapshot with trust.skills=false results in zero skills reaching the adapter for that source (T-untrusted-workspace)
    - applyWorkspaceSnapshot with trust.skills=true converts workspace drafts via buildWorkspaceSkillDefinitions and registers them
    - removeSource clears the source's skills via the adapter
    - SC1 no-main-load grep gate (T-arbitrary-exec): no dynamic loading of skill entry files on the main process path — `grep -rn "import(" src/service/aifetchlyConfig/ src/service/LocalSkillSourceAdapter.ts | grep -v "import type" | grep -vi "from \""` returns empty for skill-entry execution (the only dynamic load is inside SkillWorkerClient which runs in the utility process)
    - TRS-05 grep gate: if any skill IPC file exists, `grep -c "registerAiValidatedHandler" src/main-process/communication/*skill* 2>/dev/null || echo 0` returns 0 (skill execution IPC is non-AI-serving; expected to be a no-op assertion this plan since no new IPC is added)
    - SkillImportService.local.test.ts proves SkillWorkerClient.execute receives the entry code (utility-process routing, not main-thread loading)
    - SkillPermissionService.local.test.ts proves checkPermission fires before skill.execute on the local path
  </acceptance_criteria>
  <done>
    LocalSkillSourceAdapter provides unregister-then-register source reconciliation (bridging SkillRegistry's missing replaceSource); skills: trust-filter line drops untrusted-workspace skills before mutation; applySnapshot + removeSource wire skills through the adapter; execution-boundary + permission-gate contract tests prove the existing SkillWorkerClient/SkillPermissionService boundary holds for local skills; all grep gates green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `~/.aifetchly/skills` (user-owned) -> main process | Untrusted manifest + entry-file content crosses here; validated by buildLocalSkillDraft -> validateManifest before registration |
| `<workspace>/.aifetchly/skills` (untrusted) -> worker -> main | Raw drafts cross the worker boundary; trust.skills gate drops them before registry mutation if untrusted |
| SkillRegistry -> SkillExecutor -> SkillWorkerClient (utility process) | Skill execution crosses into a sandboxed utility process; never runs on the main thread |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Elevation of Privilege | local skill entry execution (SkillImportService.buildImportedSkillExecuteHandler) | critical | mitigate | Local JS skills route through SkillWorkerClient (Electron utilityProcess) and Python skills through PythonSkillRuntimeService per-skill venv; entry code is NEVER loaded on the main thread. Verified by SkillImportService.local.test.ts (asserts SkillWorkerClient.execute receives the code). SC1 no-main-load grep gate enforces no dynamic entry-file loading under src/service/aifetchlyConfig/ or LocalSkillSourceAdapter.ts. |
| T-18-02 | Spoofing | LocalSkillSourceAdapter (built-in name collision) | high | mitigate | SkillRegistry.registerSkill throws on duplicate name; the adapter catches the throw and emits a manifest-invalid diagnostic. Built-in skills (registered first at module load) ALWAYS win. LocalSkillSourceAdapter.test.ts covers the collision -> diagnostic -> no-crash path. |
| T-18-03 | Tampering | manifest.entry path traversal | high | mitigate | validateManifest (reused, not re-implemented) rejects entries with `..` or absolute paths per CFG-05. buildLocalSkillDraft delegates to validateManifest; buildLocalSkillDraft.test.ts covers the traversal case -> manifest-invalid. |
| T-18-04 | Elevation of Privilege | untrusted-workspace skill registration | high | mitigate | The skills: trust-filter line in applyWorkspaceSnapshot (trust.skills ? snapshot.skills : []) drops untrusted-workspace skills BEFORE applySnapshot mutates the registry (TRS-01). AIFetchlyRuntimeRegistrySync.skills.test.ts covers trust.skills=false -> zero registrations. |
| T-18-05 | Tampering | plugin command/agent registry poisoning | medium | accept (addressed in Plan 18-02) | Plugin commands/agents are registered under `plugin:<name>` source at rank 3 (lowest); built-in (rank 0), user (rank 2), workspace (rank 1) always win name collisions. The promotion code lives in Plan 18-02 Task 2; this plan does not touch the plugin path. |
| T-18-06 | Information Disclosure | sensitive data in skill arguments | medium | mitigate | SkillExecutor.validateArgs rejects API-key/password/token patterns in arguments before execution (existing code). SkillPermissionService.checkPermission fires per-call (D-SkillEnable gate-at-call). SkillPermissionService.local.test.ts proves the gate fires on the local path. |
| T-18-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs NO packages (pure integration phase, per 18-RESEARCH § Package Legitimacy Audit: N/A). No supply-chain surface added. |
</threat_model>

<verification>
- All 6 new/extended test files green: `AIFETCHLY_SKIP_TSC=1 npx vitest run --config vite.main.config.mjs test/vitest/main/service/buildLocalSkillDraft.test.ts test/vitest/main/service/AIFetchlyConfigLoader.skills.test.ts test/vitest/main/service/LocalSkillSourceAdapter.test.ts test/vitest/main/service/AIFetchlyRuntimeRegistrySync.skills.test.ts test/vitest/main/service/SkillImportService.local.test.ts test/vitest/main/service/SkillPermissionService.local.test.ts -x` + `AIFETCHLY_SKIP_TSC=1 npx vitest run --config vite.utilityCode.config.mjs test/vitest/utilitycode/workspaceWatch/WorkspaceConfigScanner.skills.test.ts -x`
- Standalone type check: `npx tsc --noEmit` (0 errors — the vitest globalSetup TSC gate is bypassed with AIFETCHLY_SKIP_TSC=1 for speed; run a standalone tsc before wave merge)
- WAT-02 worker boundary grep: `grep -E "SqliteDb|TypeORM|SkillRegistry|CommandRegistry|AgentDefinitionRegistry|electron" src/service/workspaceWatch/WorkspaceConfigScanner.ts && exit 1 || exit 0` exits 0
- SC1 no-main-load grep: no dynamic skill-entry loading under src/service/aifetchlyConfig/ or LocalSkillSourceAdapter.ts
- Regression: re-run the Phase-17 hooks test suite to confirm the AIFetchlyRuntimeRegistrySync changes (adding the skills line/block) did not regress hooks: `AIFETCHLY_SKIP_TSC=1 npx vitest run --config vite.main.config.mjs test/vitest/main/service/AIFetchlyRuntimeRegistrySync.hooks.test.ts -x` (if that file exists; otherwise the sync test suite)
</verification>

<success_criteria>
- SKL-01 fully delivered: a ~/.aifetchly/skills/<name>/manifest.json is discovered, validated, registered via the existing SkillRegistry, automatically exposed as an OpenAI tool (via the existing getAllToolFunctions path), executed via SkillExecutor -> SkillWorkerClient (never main-thread loaded), and permission-checked via SkillPermissionService.
- D-WorkspaceSkills delivered: workspace skills scanned into raw drafts (worker, WAT-02-clean), gated by trust.skills, converted main-side, registered through the adapter.
- D-SkillEnable delivered: auto-register, gate-at-call — no per-skill enable flag introduced.
- Source reconciliation works on rescan (rename/edit/delete handled via the adapter's unregister-then-register).
- All 6 STRIDE threats have a disposition; the 5 mitigated threats have a passing test.
</success_criteria>

<output>
Create `.planning/phases/18-skills-and-plugin-integration/18-01-SUMMARY.md` when done.

## Artifacts this phase (plan) produces

New symbols created by this plan (cited here so the plan-review-convergence source-grounding pass excludes them from drift verification):
- `buildLocalSkillDraft` function + `LocalSkillDraft` / `WorkspaceSkillDraft` types — `src/service/aifetchlyConfig/buildLocalSkillDraft.ts`
- `LocalSkillSourceAdapter` class (with `replaceSource(sourceId, drafts)`) — `src/service/LocalSkillSourceAdapter.ts`
- `buildWorkspaceSkillDefinitions` function — `src/service/workspaceWatch/buildWorkspaceSkillDefinitions.ts`
- `tryReadSkillFiles` method on `AIFetchlyConfigLoader` (global discovery) — `src/service/aifetchlyConfig/AIFetchlyConfigLoader.ts`
- `tryReadSkillFiles` method on `WorkspaceConfigScanner` (worker raw-draft scan) — `src/service/workspaceWatch/WorkspaceConfigScanner.ts`
- `skills:` trust-filter line + skill-registration block + skills-clear in `removeSource` — `src/service/aifetchlyConfig/AIFetchlyRuntimeRegistrySync.ts`
- `skillManifestBytes` + `maxSkillsPerSource` limits + `manifest-invalid` diagnostic code — `src/service/aifetchlyConfig/AIFetchlyConfigConstants.ts`
- Test files: buildLocalSkillDraft.test.ts, AIFetchlyConfigLoader.skills.test.ts, WorkspaceConfigScanner.skills.test.ts (utilitycode), LocalSkillSourceAdapter.test.ts, AIFetchlyRuntimeRegistrySync.skills.test.ts, SkillImportService.local.test.ts, SkillPermissionService.local.test.ts
</output>
