---
phase: 18-skills-and-plugin-integration
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/service/hooks/HookDispatcher.ts
  - src/service/PluginComponentRegistryService.ts
  - test/vitest/main/service/HookDispatcher.skillRef.test.ts
  - test/vitest/main/service/PluginComponentRegistryService.promotion.test.ts
  - test/vitest/main/service/pluginPaths.options.test.ts
autonomous: true
requirements: [SKL-02]
user_setup: []

must_haves:
  truths:
    # D-SkillRefResolve (closes Phase 17 loop)
    - "A hook whose command is skill:<name>, where <name> is a currently-registered skill, invokes that skill via SkillExecutor.execute (no longer a no-op skill-registry-not-available diagnostic)"
    - "A hook whose command is skill:<name>, where <name> is NOT registered, emits the non-fatal skill-registry-not-available diagnostic and performs NO execution (never throws, never blocks the stream) — the existing fallback path"
    # SKL-02 success criterion 2 (ROADMAP §Phase 18 SC2)
    - "Plugin commands/*.md under an enabled plugin's installPath become active slash commands in CommandRegistry under source plugin:<pluginName> with the plugin source badge (rank 3, lowest)"
    - "Plugin agents/*.md under an enabled plugin's installPath become dynamic agents in AgentDefinitionRegistry under source plugin:<pluginName> with the plugin source badge"
    - "Disabling or uninstalling a plugin reconciles its commands/agents away via replaceSource(plugin:<pluginName>, []) — no stale entries survive"
    - "Plugin commands/agents are promoted on plugin load (inside applyLoadedPlugins or a method it delegates to), NOT during the ~/.aifetchly config scan (plugins are installed packages, not config files)"
    # SKL-02 success criterion 3 (ROADMAP §Phase 18 SC3)
    - "The ~/.aifetchly/plugins/<name>/options.json path (user-home root) and the userData/plugins/installed/<pkg> package roots (app-data root) are separate filesystem trees that cannot collide by construction"
  artifacts:
    - "Rewired skill-ref branch in HookDispatcher.ts — extracts the skill name, checks SkillRegistry.isRegistered, invokes SkillExecutor.execute on hit, falls back to skillRefResult on miss"
    - "promotePluginCommandsAndAgents method (or equivalent extension of applyLoadedPlugins) on PluginComponentRegistryService — scans each enabled LoadedPlugin.installPath for commands/*.md and agents/*.md and calls CommandRegistry.replaceSource / AgentDefinitionRegistryImpl.replaceSource under plugin:<name> source IDs"
    - "Test files: HookDispatcher.skillRef.test.ts, PluginComponentRegistryService.promotion.test.ts, pluginPaths.options.test.ts"
  key_links:
    - "HookDispatcher skill-ref branch -> SkillRegistry.isRegistered(name) -> SkillExecutor.execute(name, {}, context) [registered] OR skillRefResult [unregistered]"
    - "plugin-ipc.ts (install/enable/disable/uninstall/reload) -> PluginComponentRegistryService.applyLoadedPlugins -> promotePluginCommandsAndAgents -> PluginLoaderService.loadAllPlugins -> per-plugin installPath scan -> CommandRegistry.replaceSource + AgentDefinitionRegistryImpl.replaceSource"
    - "~/.aifetchly/plugins/<name>/options.json (user-home) IS DISTINCT FROM pluginPaths.getPluginsRoot() -> userData/plugins/installed (app-data) — confirmed by filesystem-root separation, no code-level collision resolution needed"
---

<objective>
Close the Phase 17 skill-ref loop (D-SkillRefResolve) and promote plugin `commands/*.md` + `agents/*.md` into the native `CommandRegistry` / `AgentDefinitionRegistry` under `plugin:<name>` source IDs (SKL-02, D-PluginBadge). Also verify the `~/.aifetchly/plugins/<name>/options.json` path is preserved without colliding with installed plugin package roots (SKL-02 SC3).

This is an INTEGRATION plan. The hook dispatcher (Phase 17) and the plugin subsystem (`PluginLoaderService`, `PluginComponentRegistryService`, `CommandRegistry`, `AgentDefinitionRegistry`) ALREADY EXIST. Plan 18-02 rewires the skill-ref no-op to invoke a registered skill (the skill is registered by Plan 18-01), and extends `PluginComponentRegistryService.applyLoadedPlugins` to promote plugin command/agent files into the native registries.

Purpose: Satisfy SKL-02 and ROADMAP §Phase 18 SC2 + SC3, and deliver D-SkillRefResolve (the Phase 17 carry-forward that Plan 17-03 explicitly left as a documented no-op for Phase 18 to resolve).

Output: rewired HookDispatcher skill-ref branch, extended PluginComponentRegistryService promotion, and 3 TDD test files. Depends on Plan 18-01 (the skill-ref resolution invokes a skill that 18-01 registers).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-skills-and-plugin-integration/18-CONTEXT.md
@.planning/phases/18-skills-and-plugin-integration/18-RESEARCH.md
@.planning/phases/18-skills-and-plugin-integration/18-VALIDATION.md

# Plan 18-01 SUMMARY (the skill-ref branch invokes skills this plan registers)
@.planning/phases/18-skills-and-plugin-integration/18-01-SUMMARY.md

# Phase 17 carry-forward (the skill-ref no-op to rewire + the hook dispatcher)
@.planning/phases/17-hooks/17-03-SUMMARY.md
@src/service/hooks/HookDispatcher.ts
@src/service/hooks/HookRegistry.ts

# Existing skill runtime (the invocation target)
@src/config/skillsRegistry.ts
@src/service/SkillExecutor.ts

# Existing plugin subsystem (UNCHANGED — promotion target)
@src/service/PluginComponentRegistryService.ts
@src/service/PluginLoaderService.ts
@src/service/pluginPaths.ts
@src/entityTypes/pluginTypes.ts
@src/service/slashCommands/CommandRegistry.ts
@src/service/AgentDefinitionRegistry.ts

# Phase 15/16 builders reused for plugin command/agent parsing
@src/service/slashCommands/buildPromptCommandDefinition.ts
@src/service/workspaceWatch/buildAgentDefinition.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire HookDispatcher skill-ref branch to invoke registered skills (D-SkillRefResolve)</name>
  <files>
    src/service/hooks/HookDispatcher.ts,
    test/vitest/main/service/HookDispatcher.skillRef.test.ts
  </files>
  <read_first>
    - src/service/hooks/HookDispatcher.ts (lines 103-108: the skill-ref branch currently calls skillRefResult no-op; lines 175-183: skillRefResult function emitting skill-registry-not-available; line 22: SKILL_REF_COMMAND_PREFIX = "skill:"; lines 33-42: ExecuteHooksInput/HookInput shape — note hookRunId + conversationId availability for the SkillExecutor context)
    - src/service/SkillExecutor.ts (line 218: execute(name, args, context) signature — validate -> SkillPermissionService.checkPermission -> skill.execute; confirm the context shape it expects: { conversationId, toolCallId })
    - src/config/skillsRegistry.ts (isRegistered(name) + getSkill(name) — the registration check)
    - .planning/phases/17-hooks/17-03-SUMMARY.md (the documented no-op carry-forward this task closes — confirms skillRefResult was intentionally left as the Phase 18 hook point)
    - src/entityTypes/hookTypes.ts (CommandHookDefinition shape — hook.command, hook.id, hook.source)
  </read_first>
  <behavior>
    - A CommandHookDefinition with command "skill:<registeredName>" -> SkillExecutor.execute is called with that name and the result is a success HookSingleResult (no skill-registry-not-available diagnostic)
    - A CommandHookDefinition with command "skill:<unknownName>" -> the existing skillRefResult no-op fires (skill-registry-not-available diagnostic, non-fatal, never throws, never blocks) — the fallback is PRESERVED, not removed
    - The skill name extraction uses hook.command.slice(SKILL_REF_COMMAND_PREFIX.length) — robust to names containing colons beyond the prefix (per 18-RESEARCH Pattern 6)
    - SkillExecutor.execute failure (skill throws) -> a non-fatal HookSingleResult with an error carrying hook.id + hook.source (warn-mode, aggregator never blocks the stream) — mirroring how command-hook worker failures synthesize a warn result
    - The hook passes empty args {} to SkillExecutor (HookInput does not carry tool-call arguments; 18-RESEARCH Pitfall 6 — accepted limitation: hook skill-refs are best for skills with optional/no parameters)
    - The dispatcher still NEVER executes skill code in-process in the dispatcher — SkillExecutor delegates to the existing SkillWorkerClient boundary (this task adds a call to SkillExecutor, not to skill code directly)
  </behavior>
  <action>
    Rewire the skill-ref branch in HookDispatcher.ts (lines 103-108). Replace the unconditional `result = skillRefResult(hook);` with: (1) extract `const skillName = hook.command.slice(SKILL_REF_COMMAND_PREFIX.length);` (2) `if (SkillRegistry.isRegistered(skillName))` -> `const execResult = await SkillExecutor.execute(skillName, {}, { conversationId: input.conversationId ?? "", toolCallId: input.hookRunId });` then map execResult.success -> `{ hook, output: { summary: ... }, durationMs: 0 }` (or capture execResult timing if available) and !success -> `{ hook, error: { hookId: hook.id, source: hook.source, message: ... }, durationMs: 0 }`. (3) `else` -> keep `result = skillRefResult(hook);` as the FALLBACK (the skillRefResult function at lines 175-183 STAYS — it is now the unregistered-skill fallback, not the default; 18-RESEARCH Pattern 6).

    Add the SkillRegistry + SkillExecutor imports to HookDispatcher.ts (these are main-process services; HookDispatcher already runs main-side — verify no circular import with skillsRegistry, which is a leaf config module). Make the SkillExecutor invocation injectable for tests IF the existing dispatcher test seam (setClientForTests) does not cover it — prefer spying/stubbing SkillRegistry.isRegistered and SkillExecutor.execute via vi.mock or a test-only setter mirroring the existing setClientForTests pattern, rather than adding production API surface. Keep SKILL_REF_COMMAND_PREFIX unchanged (it is the Phase-17 sentinel marker).

    This task makes executeHooks await the SkillExecutor call inside the existing sequential per-hook loop (the dispatcher is already async). Non-fatal semantics: a skill failure produces a warn-mode HookSingleResult (the aggregator already handles error results without blocking — verified in the Phase 17 dispatcher).

    Per TRS-05: this dispatcher path is invoked from the chat tool loop (StreamEventProcessor) which is already AI-gated downstream; no new IPC handler is added here.
  </action>
  <verify>
    <automated>AIFETCHLY_SKIP_TSC=1 npx vitest run --config vite.main.config.mjs test/vitest/main/service/HookDispatcher.skillRef.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - HookDispatcher.skillRef.test.ts exits 0 (RED->GREEN)
    - skillRefResult function is STILL defined (the fallback is preserved): `grep -c "function skillRefResult" src/service/hooks/HookDispatcher.ts` returns 1
    - The skill-ref branch now calls SkillExecutor.execute for registered skills: `grep -c "SkillExecutor.execute\|SkillRegistry.isRegistered" src/service/hooks/HookDispatcher.ts` returns >= 2
    - Registered-skill case: a hook with command "skill:<registeredName>" produces a success result and does NOT emit skill-registry-not-available
    - Unregistered-skill case: a hook with command "skill:<unknownName>" produces the skill-registry-not-available diagnostic (non-fatal, never throws)
    - SkillExecutor failure case: a registered skill whose execute rejects produces a HookSingleResult with an error (warn-mode), and executeHooks still resolves (never throws)
    - Regression: Phase 17 HookDispatcher tests still pass — `AIFETCHLY_SKIP_TSC=1 npx vitest run --config vite.main.config.mjs test/vitest/main/service/HookDispatcher -x` (re-run the existing dispatcher suite to confirm callback + command + trust paths are unchanged)
  </acceptance_criteria>
  <done>
    D-SkillRefResolve delivered: the skill-ref branch invokes registered skills via SkillExecutor.execute and falls back to the preserved skillRefResult no-op for unregistered names. Phase 17 loop closed. All skill-ref tests + Phase 17 dispatcher regression green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Promote plugin commands/agents into native registries on plugin load (SKL-02, D-PluginBadge)</name>
  <files>
    src/service/PluginComponentRegistryService.ts,
    test/vitest/main/service/PluginComponentRegistryService.promotion.test.ts
  </files>
  <read_first>
    - src/service/PluginComponentRegistryService.ts (applyLoadedPlugins at line 31 — currently just PluginRuntimeCache.clear; unregisterPluginCapabilities at line 39; reload at line 47 — the three plugin-lifecycle hook points this task extends)
    - src/service/PluginLoaderService.ts (loadAllPlugins at line 75 — memoized; LoadedPlugin shape at line 50: installPath + enabled + health/missing_files; confirm how enabled state and missing_files health are represented so the promotion can skip disabled or missing-file plugins per 18-RESEARCH Pitfall 5)
    - src/service/slashCommands/CommandRegistry.ts (replaceSource at line 83 — the atomic reconcile; SOURCE_RANK at line 26: built-in(0) > workspace(1) > user(2) > plugin(3) — plugin is rank 3 lowest, T-plugin-poison mitigation)
    - src/service/AgentDefinitionRegistry.ts (replaceSource at line 219; SOURCE_RANK at line 102 — plugin rank reserved)
    - src/service/slashCommands/buildPromptCommandDefinition.ts (Phase 15 — the CMD-06 single-owner validator reused for plugin commands; confirm its sourceMeta shape so plugin commands pass source "plugin" + pluginName)
    - src/service/workspaceWatch/buildAgentDefinition.ts (Phase 16 — the AGT-02 validator reused for plugin agents)
    - src/service/pluginPaths.ts (getPluginsRoot at line 15 -> userData/plugins/installed; confirm plugin install dirs live there, distinct from ~/.aifetchly/plugins)
    - src/entityTypes/pluginTypes.ts (PluginManifest at line 28 — has skills/mcpServers but NO commands/agents field, confirming commands/agents are discovered by FILESCAN not manifest declaration per 18-RESEARCH Pattern 5 / Assumption A3)
    - src/main-process/communication/plugin-ipc.ts (confirm applyLoadedPlugins is called at the install/enable/disable/uninstall/reload sites — 18-RESEARCH cites lines 279/338/349/383)
  </read_first>
  <behavior>
    - An enabled plugin with installPath/commands/review.md (valid CMD-06 frontmatter) -> CommandRegistry.replaceSource("plugin:<pluginName>", [reviewDef]) is called and /review becomes active with the plugin source badge
    - An enabled plugin with installPath/agents/researcher.md (valid AGT-02 frontmatter) -> AgentDefinitionRegistryImpl.replaceSource("plugin:<pluginName>", [researcherDef]) is called and the agent is listable
    - A plugin whose commands collide by name with a built-in/user/workspace command -> the plugin entry loses (rank 3 lowest; T-plugin-poison) — verified via SOURCE_RANK, not new code
    - A disabled or uninstalled plugin -> replaceSource("plugin:<pluginName>", []) reconciles its entries away (zero stale commands/agents)
    - A plugin with missing_files health (install dir absent) -> promotion SKIPS it (no throw, no stale entries; 18-RESEARCH Pitfall 5)
    - applyLoadedPlugins still clears the PluginRuntimeCache (existing behavior preserved) AND additionally runs the promotion
    - The promotion reads ONLY commands/*.md and agents/*.md from each enabled plugin's installPath — it does NOT scan the whole install dir, and does NOT touch ~/.aifetchly (plugins are installed packages, not config files)
  </behavior>
  <action>
    Extend PluginComponentRegistryService.applyLoadedPlugins (line 31) to additionally call a new private/static method `promotePluginCommandsAndAgents()`. Keep the existing `PluginRuntimeCache.clear("apply-loaded-plugins")` call as the first line (preserve existing behavior). The new method:

    (1) Call `PluginLoaderService.loadAllPlugins()` (cache hit is fine — it is memoized and applyLoadedPlugins is the invalidation hook). (2) For each LoadedPlugin: skip if not enabled OR if health indicates missing_files (per Pitfall 5 — fs.existsSync(installPath) guard or the loader's health field). (3) Scan `<installPath>/commands/*.md` — for each file: stat (CFG-04 commandMdBytes cap), read, run the restricted frontmatter parser (the Phase 13/15 parser), call buildPromptCommandDefinition with sourceMeta `{ source: "plugin", sourceId: "plugin:<pluginName>", pluginName, relativePath }` — collect ok definitions, collect !ok diagnostics. (4) Call `CommandRegistry.replaceSource("plugin:<pluginName>", commandDefs)` — atomic reconcile handles add/change/delete/rename in one call (per 18-RESEARCH Pattern 5). (5) Scan `<installPath>/agents/*.md` — same pattern via buildAgentDefinition with the plugin sourceMeta. (6) Call `AgentDefinitionRegistryImpl.replaceSource("plugin:<pluginName>", agentDefs)`. (7) For plugins that are disabled/uninstalled/missing, call `replaceSource("plugin:<pluginName>", [])` for BOTH registries to drop their entries (the loader returns them in a disabled/absent state; or track a prior-sourceId set and reconcile — prefer letting the loader's enabled filter drive it and emitting [] for the disabled ones).

    The scoped sourceId is exactly `plugin:<pluginName>` (D-PluginBadge) — matching the SOURCE_RANK reservations on both registries (rank 3, lowest — built-in/user/workspace always win name collisions; T-plugin-poison mitigation is structural, no extra code). The plugin source badge is the EXISTING "plugin" value in SlashCommandSource / agent source fields (aifetchlyConfigTypes.ts line 78) — no new badge enum.

    Per 18-RESEARCH Pattern 5 / Assumption A3: plugin commands/agents are NOT declared in PluginManifest (it has skills/mcpServers only); they are discovered by filesystem scan of installPath/commands + installPath/agents. If the directory does not exist, that is the happy path (no diagnostic, empty replaceSource for that registry). Reuse the existing restricted frontmatter parser + buildPromptCommandDefinition + buildAgentDefinition — do NOT write a new parser.

    Confirm unregisterPluginCapabilities (line 39) also clears the plugin's commands/agents (call replaceSource("plugin:<name>", []) for both registries) so the disable/uninstall IPC path reconciles even if applyLoadedPlugins is not re-invoked. Keep reload (line 47) calling applyLoadedPlugins (which now re-promotes after the cache clear + reload).

    Per CLAUDE.md auto-commit rule: commit after this method is complete.
  </action>
  <verify>
    <automated>AIFETCHLY_SKIP_TSC=1 npx vitest run --config vite.main.config.mjs test/vitest/main/service/PluginComponentRegistryService.promotion.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - PluginComponentRegistryService.promotion.test.ts exits 0 (RED->GREEN)
    - applyLoadedPlugins still clears the cache AND promotes: `grep -c "PluginRuntimeCache.clear\|promotePluginCommandsAndAgents" src/service/PluginComponentRegistryService.ts` returns >= 2
    - CommandRegistry.replaceSource is called with sourceId "plugin:<pluginName>" for plugin commands: verified by test assertion (spy on CommandRegistry.replaceSource)
    - AgentDefinitionRegistryImpl.replaceSource is called with sourceId "plugin:<pluginName>" for plugin agents: verified by test assertion
    - A disabled/uninstalled plugin -> replaceSource("plugin:<pluginName>", []) for both registries (no stale entries)
    - T-plugin-poison: a plugin command whose name collides with a built-in -> the built-in wins (verified by checking SOURCE_RANK precedence, not new code; the test asserts the registry's resolved command is the built-in)
    - A plugin with a missing install dir -> promotion skips it without throwing (Pitfall 5)
    - The promotion does NOT scan ~/.aifetchly (it scans LoadedPlugin.installPath only): verified by test setup using a tmp install dir, not the config root
  </acceptance_criteria>
  <done>
    Plugin commands/*.md and agents/*.md are promoted into CommandRegistry / AgentDefinitionRegistry under plugin:<name> source IDs (rank 3, lowest) on every plugin load/enable/disable/uninstall/reload. D-PluginBadge delivered via the existing "plugin" source value. T-plugin-poison structurally mitigated by SOURCE_RANK. All promotion tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify options.json path non-collision + i18n plugin-badge check (SKL-02 SC3)</name>
  <files>
    test/vitest/main/service/pluginPaths.options.test.ts,
    src/views/lang/en.ts,
    src/views/lang/zh.ts,
    src/views/lang/es.ts,
    src/views/lang/fr.ts,
    src/views/lang/de.ts,
    src/views/lang/ja.ts
  </files>
  <read_first>
    - src/service/pluginPaths.ts (getPluginsRoot at line 15 -> path.join(getElectronUserDataPath(), "plugins", "installed"); confirm it ONLY resolves the userData root and never touches ~/.aifetchly/plugins)
    - src/service/SkillEnvironmentManager.ts (getElectronUserDataPath at line 31 -> Electron app.getPath('userData'); the app-data root, distinct from os.homedir())
    - src/service/aifetchlyConfig/AIFetchlyConfigConstants.ts (AIFETCHLY_CONFIG_DIR_NAME = ".aifetchly" at line 21; the user-home root literal)
    - src/entityTypes/aifetchlyConfigTypes.ts (AIFetchlyConfigFileKind includes "plugin-options" at line 36 — already reserved; the config loader reads ~/.aifetchly/plugins/<name>/options.json under this kind)
    - src/views/lang/en.ts (search for an existing "plugin" badge label under aifetchlyConfig / slashCommands groups — Phase 13 may have added it; if present, this task adds nothing to lang files)
    - docs/prd/aifetchly-local-extensibility-prd.md section 6.3 (the options.json path-preservation requirement this task verifies)
  </read_first>
  <behavior>
    - pluginPaths.getPluginsRoot() resolves to a path ending in "plugins/installed" under Electron's userData dir (app-data root) — NOT under the user home
    - The ~/.aifetchly/plugins/<name>/options.json path resolves under os.homedir() + ".aifetchly" (user-home root) — a DIFFERENT filesystem tree
    - The two roots cannot collide by construction (app-data != user-home on every supported platform)
    - pluginPaths.ts does NOT resolve ~/.aifetchly/plugins (it only resolves the userData/plugins/installed root)
    - IF a "Plugin" badge chrome string is missing from the i18n groups (only if the suggestions/status UI needs a new label and Phase 13 did not add one): all 6 lang files (en/zh/es/fr/de/ja) gain the key under the aifetchlyConfig or slashCommands group. IF Phase 13 already added it: this task's lang-file diff is empty and only the test ships.
  </behavior>
  <action>
    Create `test/vitest/main/service/pluginPaths.options.test.ts` as a characterization test proving the SKL-02 SC3 / PRD section 6.3 path-preservation invariant. Assert: (1) `pluginPaths.getPluginsRoot()` resolves to a string ending in `path.join("plugins", "installed")` that is UNDER `app.getPath('userData')` (mock Electron's app.getPath or use the existing test pattern for pluginPaths). (2) The user-home config root `path.join(os.homedir(), ".aifetchly")` is a DIFFERENT directory than the userData root — assert they are not equal and that `path.relative` between them does not indicate containment. (3) A constructed `~/.aifetchly/plugins/<name>/options.json` path does NOT start with `pluginPaths.getPluginsRoot()` (no prefix overlap). (4) Assert pluginPaths.ts exports NO function that resolves under `~/.aifetchly/plugins` (grep the module surface — getPluginsRoot and siblings all resolve under userData).

    i18n check: grep `src/views/lang/en.ts` for an existing plugin-badge label (e.g. "Plugin" under the slashCommands source-badge group or aifetchlyConfig group). If a label already exists (Phase 13 CMD-05 source-badge work may have added it), make NO lang-file change — note in the task commit that the badge label is already present. If it is absent AND the promotion surfaces a user-visible "Plugin" label that needs translation, add the key to all 6 lang files (en/zh/es/fr/de/ja) under the appropriate group with accurate translations — but ONLY if a new chrome string is genuinely needed (the `plugin` source value is DATA flowing through the existing badge mechanism; the renderer CMD-05 UI is still pending and may own the label). Default to: if no new user-visible string is introduced by this backend promotion, skip the lang-file edit and ship only the test.

    Per 18-RESEARCH Pattern 4 / Discretion Item 4: the options.json conflict mechanism is filesystem-root separation — NO code-level collision resolution is needed. This task PROVES that invariant with a test; it does not add collision-resolution code.
  </action>
  <verify>
    <automated>AIFETCHLY_SKIP_TSC=1 npx vitest run --config vite.main.config.mjs test/vitest/main/service/pluginPaths.options.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - pluginPaths.options.test.ts exits 0 (RED->GREEN)
    - The test asserts pluginPaths.getPluginsRoot() is under the Electron userData dir and is distinct from path.join(os.homedir(), ".aifetchly")
    - The test asserts a ~/.aifetchly/plugins/<name>/options.json path does NOT start with pluginPaths.getPluginsRoot()
    - pluginPaths.ts is UNCHANGED (no collision-resolution code added): `git diff --name-only src/service/pluginPaths.ts` returns empty (or only whitespace/comments if touched)
    - IF i18n keys were added: `grep -c "plugin" src/views/lang/en.ts src/views/lang/zh.ts src/views/lang/es.ts src/views/lang/fr.ts src/views/lang/de.ts src/views/lang/ja.ts` shows the new key present in ALL 6 files; IF no new string was needed, lang files are unchanged (documented in the commit)
  </acceptance_criteria>
  <done>
    SKL-02 SC3 delivered: the ~/.aifetchly/plugins/<name>/options.json path is proven non-colliding with userData/plugins/installed via filesystem-root separation (PRD section 6.3 invariant held). i18n either unchanged (label pre-existing) or extended across all 6 lang files if a new badge string was needed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin install dir (installed package) -> CommandRegistry / AgentDefinitionRegistry | Plugin-authored markdown crosses here; parsed by the existing CMD-06/AGT-02 validators and registered at the lowest source rank |
| Hook dispatcher skill-ref branch -> SkillExecutor -> SkillRegistry | A hook-declared skill reference crosses here; resolved only if the skill is registered (Plan 18-01), else non-fatal no-op |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Elevation of Privilege | local skill entry execution | critical | mitigate (Plan 18-01 owns the primary mitigation; this plan consumes it) | The skill-ref branch invokes SkillExecutor.execute which delegates to the existing SkillWorkerClient utility-process boundary (Plan 18-01 verified the boundary holds). The dispatcher NEVER executes skill code in-process — it calls SkillExecutor, which routes to SkillWorkerClient. HookDispatcher.skillRef.test.ts confirms the invocation path. |
| T-18-02 | Spoofing | skill-ref name resolution | medium | mitigate | The skill-ref branch checks SkillRegistry.isRegistered(name) BEFORE invoking; unregistered names fall back to the non-fatal skill-registry-not-available no-op. A hook cannot invoke a skill that is not registered. Built-in skills always win name collisions (Plan 18-01 adapter). |
| T-18-03 | Tampering | plugin command path traversal | low | accept | Plugin commands are parsed by the existing buildPromptCommandDefinition (Phase 15) which enforces CMD-06 schema; plugin install dirs are trusted package roots (installed via the existing PluginImportService). No new path handling. |
| T-18-04 | Elevation of Privilege | untrusted-workspace skill via hook | high | mitigate (Plan 18-01 owns the trust gate) | The skill-ref branch can only invoke a skill that is ALREADY registered in SkillRegistry; Plan 18-01's skills: trust-filter line ensures untrusted-workspace skills never reach the registry. Therefore an untrusted-workspace skill is never invokable via a hook ref. |
| T-18-05 | Tampering | plugin command/agent registry poisoning | high | mitigate | Plugin commands/agents register under `plugin:<name>` source at SOURCE_RANK rank 3 (lowest) on both CommandRegistry and AgentDefinitionRegistry. Built-in (rank 0), workspace (rank 1), and user (rank 2) commands/agents ALWAYS win name collisions. PluginComponentRegistryService.promotion.test.ts asserts the precedence. Disabled/uninstalled plugins reconcile via replaceSource("plugin:<name>", []). |
| T-18-06 | Information Disclosure | sensitive data via skill args | medium | accept (existing SkillExecutor mitigation) | Hook skill-refs pass empty args {} (18-RESEARCH Pitfall 6 — HookInput carries no tool args), so no sensitive data flows through the hook->skill path. The AI-tool-invocation path (Plan 18-01) is where args flow, and SkillExecutor.validateArgs already rejects sensitive patterns there. |
| T-18-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs NO packages (pure integration phase, per 18-RESEARCH § Package Legitimacy Audit: N/A). No supply-chain surface added. |
</threat_model>

<verification>
- All 3 new test files green: `AIFETCHLY_SKIP_TSC=1 npx vitest run --config vite.main.config.mjs test/vitest/main/service/HookDispatcher.skillRef.test.ts test/vitest/main/service/PluginComponentRegistryService.promotion.test.ts test/vitest/main/service/pluginPaths.options.test.ts -x`
- Standalone type check: `npx tsc --noEmit` (0 errors before wave merge)
- Phase 17 regression: `AIFETCHLY_SKIP_TSC=1 npx vitest run --config vite.main.config.mjs test/vitest/main/service/HookDispatcher -x` (existing dispatcher suite — confirms callback/command/trust paths unchanged)
- Phase 15/16 regression: re-run CommandRegistry + AgentDefinitionRegistry tests to confirm the plugin-rank-3 replaceSource did not regress existing sources
- Manual UAT (SC2): install a test plugin with commands/*.md + agents/*.md, observe its /command appears in suggestions and the agent is listable, with the plugin source badge (per 18-VALIDATION.md Manual-Only Verifications)
</verification>

<success_criteria>
- D-SkillRefResolve delivered: a registered skill is invokable via a hook skill:<name> ref; unregistered names fall back to the preserved non-fatal skill-registry-not-available no-op. Phase 17 loop closed.
- SKL-02 SC2 delivered: plugin commands/*.md become active slash commands and plugin agents/*.md become dynamic agents under plugin:<name> source IDs (rank 3, lowest) on every plugin lifecycle event.
- SKL-02 SC3 delivered: ~/.aifetchly/plugins/<name>/options.json path proven non-colliding with userData/plugins/installed (filesystem-root separation).
- D-PluginBadge delivered via the existing "plugin" source value.
- All 6 STRIDE threats have a disposition; mitigated threats have a passing test or structural guarantee.
</success_criteria>

<output>
Create `.planning/phases/18-skills-and-plugin-integration/18-02-SUMMARY.md` when done.

## Artifacts this phase (plan) produces

New/modified symbols created by this plan (cited here so the plan-review-convergence source-grounding pass excludes them from drift verification):
- Rewired skill-ref branch in `HookDispatcher.executeHooks` (SkillRegistry.isRegistered -> SkillExecutor.execute, fallback to skillRefResult) — `src/service/hooks/HookDispatcher.ts`
- New `promotePluginCommandsAndAgents` method (static or private) on `PluginComponentRegistryService` + extension of `applyLoadedPlugins` / `unregisterPluginCapabilities` to call it — `src/service/PluginComponentRegistryService.ts`
- Test files: HookDispatcher.skillRef.test.ts, PluginComponentRegistryService.promotion.test.ts, pluginPaths.options.test.ts
- (Conditional) i18n "Plugin" badge label across all 6 lang files IF a new chrome string was needed — `src/views/lang/{en,zh,es,fr,de,ja}.ts`
</output>
